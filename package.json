{
  "name": "durablerun",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "packageManager": "pnpm@10.29.1",
  "scripts": {
    "typecheck": "pnpm -r --parallel exec tsc --noEmit",
    "test": "vitest run",
    "lint": "biome lint .",
    "format": "biome format --write .",
    "format-check": "biome format .",
    "lint:determinism": "bash scripts/determinism-lint.sh",
    "lint:user-boundary": "bash scripts/user-boundary-lint.sh",
    "verify": "pnpm lint && pnpm lint:determinism && pnpm lint:user-boundary && pnpm lint:ledger && pnpm lint:fragments && pnpm format-check && pnpm typecheck && pnpm test",
    "lint:ledger": "python3 scripts/spec-ledger.py",
    "lint:fragments": "python3 scripts/fragment-lint.py",
    "verify:tla": "bash scripts/confine.sh bash scripts/tla.sh",
    "verify:fuzz": "FUZZ_SEEDS=2000 FUZZ_STEPS=100 bash scripts/confine.sh pnpm exec vitest run packages/conformance/test",
    "verify:fuzz:deep": "FUZZ_SEEDS=100000 FUZZ_STEPS=150 bash scripts/confine.sh pnpm exec vitest run packages/conformance/test"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "tsx": "^4.23.1",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
