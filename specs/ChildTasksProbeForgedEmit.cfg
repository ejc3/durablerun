\* Vacuity probe, EXPECTED TO FAIL: DoneIsFirstOutcome has a counterexample when
\* a user emit may write the completion event's name.
CONSTANTS
  MaxRetries = 1
  SameQueue = TRUE
  SameQueueRule = "allow"
  AtomicEmit = TRUE
  UserMayForge = TRUE
SPECIFICATION Spec
INVARIANT DoneIsFirstOutcome
CHECK_DEADLOCK FALSE
