{
  "spawn": {
    "spawned": [
      {
        "sql": "insert into `tasks` (`task_id`, `queue`, `task_name`, `params`, `headers`, `retry_strategy`, `max_attempts`, `cancellation`, `idempotency_key`, `state`, `enqueue_at_ms`, `cancel_at_ms`, `created_at_ms`, `fence_stamp`, `fence_at_ms`) select * from (select ? as `task_id`, ? as `queue`, ? as `task_name`, ? as `params`, ? as `headers`, ? as `retry_strategy`, ? as `max_attempts`, ? as `cancellation`, ? as `idempotency_key`, ? as `state`, (COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n) + ?) as `enqueue_at_ms`, (COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n) + CAST(? AS SIGNED) + CAST(? AS SIGNED)) as `cancel_at_ms`, COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n) as `created_at_ms`, ? as `fence_stamp`, COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n) as `fence_at_ms` where (NOT EXISTS (SELECT 1 FROM tasks x WHERE x.task_id = ?)\n         AND NOT EXISTS (SELECT 1 FROM runs r WHERE r.task_id = ?)) and (((COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n)) IS NOT NULL\n    AND (COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n)) BETWEEN 0 AND 253402300799000\n    AND (COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n)) <= 253402300799000 - (CAST((?) AS SIGNED)))) and (CAST(? AS SIGNED) IS NULL OR ((COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n)) IS NOT NULL\n    AND (COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n)) BETWEEN 0 AND 253402300799000\n    AND (COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n)) <= 253402300799000 - (CAST((?) AS SIGNED) + CAST((?) AS SIGNED))))) as `excluded` on duplicate key update `queue` = `tasks`.`queue`",
        "bindArity": 20
      },
      {
        "sql": "insert into `runs` (`run_id`, `queue`, `task_id`, `attempt`, `state`, `available_at_ms`, `created_at_ms`, `fence_stamp`, `fence_at_ms`) select ? as `run_id`, `f`.`queue` as `queue`, `f`.`task_id` as `task_id`, ? as `attempt`, ? as `state`, `f`.`enqueue_at_ms` as `available_at_ms`, `f`.`fence_at_ms` as `created_at_ms`, ? as `fence_stamp`, `f`.`fence_at_ms` as `fence_at_ms` from `tasks` as `f` where `f`.`task_id` = ? and `f`.`fence_stamp` = ? and (((f.enqueue_at_ms) IS NOT NULL AND f.enqueue_at_ms BETWEEN 0 AND 253402300799000)) and not exists (select `r`.`run_id` from `runs` as `r` where `r`.`task_id` = `f`.`task_id`)",
        "bindArity": 6
      },
      {
        "sql": "select `t`.`task_id` as `task_id`, (select `r`.`run_id` from `runs` as `r` where (t.task_id = r.task_id AND t.queue = r.queue) order by `r`.`attempt` desc limit ?) as `run_id` from `tasks` as `t` where ((t.task_id = ? AND t.queue = ?)\n         OR (CAST(? AS CHAR) IS NOT NULL AND t.queue = ? AND t.idempotency_key = ?\n           AND t.task_id <> ?)) order by case when `t`.`task_id` = ? then 1 else 0 end, `t`.`task_id` limit ?",
        "bindArity": 9
      }
    ],
    "spawned-child": [
      {
        "sql": "insert into `tasks` (`task_id`, `queue`, `task_name`, `params`, `headers`, `retry_strategy`, `max_attempts`, `cancellation`, `idempotency_key`, `state`, `enqueue_at_ms`, `cancel_at_ms`, `created_at_ms`, `fence_stamp`, `fence_at_ms`) select * from (select ? as `task_id`, ? as `queue`, ? as `task_name`, ? as `params`, ? as `headers`, ? as `retry_strategy`, ? as `max_attempts`, ? as `cancellation`, ? as `idempotency_key`, ? as `state`, (COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n) + ?) as `enqueue_at_ms`, (COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n) + CAST(? AS SIGNED) + CAST(? AS SIGNED)) as `cancel_at_ms`, COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n) as `created_at_ms`, ? as `fence_stamp`, COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n) as `fence_at_ms` where (NOT EXISTS (SELECT 1 FROM tasks x WHERE x.task_id = ?)\n         AND NOT EXISTS (SELECT 1 FROM runs r WHERE r.task_id = ?)) and (((COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n)) IS NOT NULL\n    AND (COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n)) BETWEEN 0 AND 253402300799000\n    AND (COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n)) <= 253402300799000 - (CAST((?) AS SIGNED)))) and (CAST(? AS SIGNED) IS NULL OR ((COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n)) IS NOT NULL\n    AND (COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n)) BETWEEN 0 AND 253402300799000\n    AND (COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n)) <= 253402300799000 - (CAST((?) AS SIGNED) + CAST((?) AS SIGNED)))) and exists (select `r`.`run_id` from `runs` as `r` inner join `tasks` as `t` on (t.task_id = r.task_id AND t.queue = r.queue) where `r`.`run_id` = ? and `r`.`queue` = ? and `r`.`task_id` = ? and `r`.`claimed_by` = ? and `r`.`state` = ? and (t.state IN ('pending','running','sleeping')))) as `excluded` on duplicate key update `queue` = `tasks`.`queue`",
        "bindArity": 25
      },
      {
        "sql": "insert into `runs` (`run_id`, `queue`, `task_id`, `attempt`, `state`, `available_at_ms`, `created_at_ms`, `fence_stamp`, `fence_at_ms`) select ? as `run_id`, `f`.`queue` as `queue`, `f`.`task_id` as `task_id`, ? as `attempt`, ? as `state`, `f`.`enqueue_at_ms` as `available_at_ms`, `f`.`fence_at_ms` as `created_at_ms`, ? as `fence_stamp`, `f`.`fence_at_ms` as `fence_at_ms` from `tasks` as `f` where `f`.`task_id` = ? and `f`.`fence_stamp` = ? and (((f.enqueue_at_ms) IS NOT NULL AND f.enqueue_at_ms BETWEEN 0 AND 253402300799000)) and not exists (select `r`.`run_id` from `runs` as `r` where `r`.`task_id` = `f`.`task_id`)",
        "bindArity": 6
      },
      {
        "sql": "select `t`.`task_id` as `task_id`, (select `r`.`run_id` from `runs` as `r` where (t.task_id = r.task_id AND t.queue = r.queue) order by `r`.`attempt` desc limit ?) as `run_id` from `tasks` as `t` where ((t.task_id = ? AND t.queue = ?)\n         OR (CAST(? AS CHAR) IS NOT NULL AND t.queue = ? AND t.idempotency_key = ?\n           AND t.task_id <> ?)) order by case when `t`.`task_id` = ? then 1 else 0 end, `t`.`task_id` limit ?",
        "bindArity": 9
      }
    ]
  },
  "claim": {
    "claimed": [
      {
        "sql": "update `runs` set `state` = ?, `claimed_by` = ?, `claim_gen` = `claim_gen` + ?, `lease_ms` = ?, `claim_expires_at_ms` = (COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n) + ?), `heartbeat_at_ms` = COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n), `wake_step` = coalesce(`wake_step`, ((SELECT MIN(w.step_name)\n           FROM waits w\n           WHERE w.run_id = runs.run_id\n      AND w.queue = runs.queue\n      AND w.task_id = runs.task_id\n      AND w.event_name = runs.wake_event\n      AND w.status = 'waiting'\n      AND w.timeout_at_ms <=> runs.available_at_ms\n      AND (runs.available_at_ms IS NULL\n        OR ((runs.available_at_ms) IS NOT NULL AND runs.available_at_ms BETWEEN 0 AND 253402300799000))\n      AND (w.timeout_at_ms IS NULL\n        OR ((w.timeout_at_ms) IS NOT NULL AND w.timeout_at_ms BETWEEN 0 AND 253402300799000))\n           GROUP BY w.run_id\n            HAVING COUNT(*) = 1))), `fence_stamp` = ?, `fence_at_ms` = COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n) where `run_id` in (\n         SELECT run_id FROM (\n           SELECT legs.run_id FROM (\n             (SELECT r.run_id, r.available_at_ms\n        FROM runs r FORCE INDEX (runs_poll)\n        WHERE r.queue = ? AND r.state = 'pending'\n          AND (((r.available_at_ms) IS NOT NULL AND r.available_at_ms BETWEEN 0 AND 253402300799000)\n    AND r.available_at_ms <= COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n))\n          AND EXISTS (SELECT 1 FROM tasks t\n                      WHERE t.task_id = r.task_id AND t.queue = r.queue AND t.state IN ('pending','running','sleeping') AND (t.cancel_at_ms IS NULL\n    OR (((t.cancel_at_ms) IS NOT NULL AND t.cancel_at_ms BETWEEN 0 AND 253402300799000)\n      AND t.cancel_at_ms > COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n)))\n               AND (\n    CASE\n      WHEN NOT JSON_VALID(t.retry_strategy) THEN 0\n      WHEN JSON_TYPE(CAST(t.retry_strategy AS JSON)) <> 'OBJECT' THEN 0\n      WHEN JSON_TYPE(JSON_EXTRACT(t.retry_strategy, '$.kind')) <> 'STRING' THEN 0\n      WHEN (JSON_UNQUOTE(JSON_EXTRACT(t.retry_strategy, '$.kind'))) = 'none' THEN 1\n      WHEN (JSON_UNQUOTE(JSON_EXTRACT(t.retry_strategy, '$.kind'))) = 'fixed' THEN CASE WHEN (CASE\n      WHEN JSON_TYPE(JSON_EXTRACT(t.retry_strategy, '$.baseSeconds')) NOT IN ('INTEGER','UNSIGNED INTEGER','DOUBLE','DECIMAL') THEN FALSE\n      WHEN CAST(JSON_EXTRACT(t.retry_strategy, '$.baseSeconds') AS DOUBLE) < 0 OR CAST(JSON_EXTRACT(t.retry_strategy, '$.baseSeconds') AS DOUBLE) > 3155760000000 THEN FALSE\n      ELSE ROUND(CAST(JSON_EXTRACT(t.retry_strategy, '$.baseSeconds') AS DOUBLE) * 1000) BETWEEN 0 AND 3155760000000\n    END) THEN 1 ELSE 0 END\n      WHEN (JSON_UNQUOTE(JSON_EXTRACT(t.retry_strategy, '$.kind'))) = 'exponential' THEN CASE\n        WHEN (CASE\n      WHEN JSON_TYPE(JSON_EXTRACT(t.retry_strategy, '$.baseSeconds')) NOT IN ('INTEGER','UNSIGNED INTEGER','DOUBLE','DECIMAL') THEN FALSE\n      WHEN CAST(JSON_EXTRACT(t.retry_strategy, '$.baseSeconds') AS DOUBLE) < 0 OR CAST(JSON_EXTRACT(t.retry_strategy, '$.baseSeconds') AS DOUBLE) > 3155760000000 THEN FALSE\n      ELSE ROUND(CAST(JSON_EXTRACT(t.retry_strategy, '$.baseSeconds') AS DOUBLE) * 1000) BETWEEN 0 AND 3155760000000\n    END)\n          AND (CASE\n      WHEN JSON_TYPE(JSON_EXTRACT(t.retry_strategy, '$.factor')) NOT IN ('INTEGER','UNSIGNED INTEGER','DOUBLE','DECIMAL') THEN FALSE\n      ELSE CAST(JSON_EXTRACT(t.retry_strategy, '$.factor') AS DOUBLE) BETWEEN 0 AND 1.7976931348623157e308\n    END)\n          AND (CASE\n      WHEN JSON_TYPE(JSON_EXTRACT(t.retry_strategy, '$.maxSeconds')) NOT IN ('INTEGER','UNSIGNED INTEGER','DOUBLE','DECIMAL') THEN FALSE\n      WHEN CAST(JSON_EXTRACT(t.retry_strategy, '$.maxSeconds') AS DOUBLE) < 0 OR CAST(JSON_EXTRACT(t.retry_strategy, '$.maxSeconds') AS DOUBLE) > 3155760000000 THEN FALSE\n      ELSE ROUND(CAST(JSON_EXTRACT(t.retry_strategy, '$.maxSeconds') AS DOUBLE) * 1000) BETWEEN 0 AND 3155760000000\n    END)\n        THEN 1 ELSE 0 END\n      ELSE 0\n    END = 1\n  )\n               AND (\n    CASE\n      WHEN t.headers IS NULL THEN 1\n      WHEN NOT JSON_VALID(t.headers) THEN 0\n      WHEN JSON_TYPE(CAST(t.headers AS JSON)) <> 'OBJECT' THEN 0\n      WHEN EXISTS (\n        SELECT 1 FROM JSON_TABLE(t.headers, '$.*' COLUMNS (member JSON PATH '$')) h\n        WHERE JSON_TYPE(h.member) <> 'STRING'\n      ) THEN 0\n      ELSE 1\n    END = 1\n  )\n               AND NOT EXISTS (SELECT 1 FROM (SELECT sibling_run.* FROM runs sibling_run\n    WHERE sibling_run.task_id = r.task_id) sibling\n               WHERE sibling.state IN ('pending','running','sleeping')\n                 AND sibling.run_id <> r.run_id)\n               AND (r.wake_step IS NOT NULL OR NOT EXISTS (SELECT 1 FROM waits w\n                          WHERE w.run_id = r.run_id\n      AND w.queue = r.queue\n      AND w.task_id = r.task_id\n      AND w.event_name = r.wake_event\n      AND w.status = 'waiting'\n      AND w.timeout_at_ms <=> r.available_at_ms\n      AND (r.available_at_ms IS NULL\n        OR ((r.available_at_ms) IS NOT NULL AND r.available_at_ms BETWEEN 0 AND 253402300799000))\n      AND (w.timeout_at_ms IS NULL\n        OR ((w.timeout_at_ms) IS NOT NULL AND w.timeout_at_ms BETWEEN 0 AND 253402300799000))\n                          GROUP BY w.run_id\n                              HAVING COUNT(*) > 1))\n               AND NOT EXISTS (\n      SELECT 1 FROM waits w\n      WHERE w.run_id = r.run_id AND w.status = 'waiting'\n        AND w.timeout_at_ms IS NOT NULL\n        AND NOT ((w.timeout_at_ms) IS NOT NULL AND w.timeout_at_ms BETWEEN 0 AND 253402300799000)\n    )\n               AND ((r.claim_gen) IS NOT NULL AND r.claim_gen BETWEEN 0 AND 999999)\n               AND ((r.activated_gen) IS NOT NULL AND r.activated_gen BETWEEN 0 AND 1000000)\n               AND r.activated_gen <= r.claim_gen\n               AND ((r.relaunch_count) IS NOT NULL AND r.relaunch_count BETWEEN 0 AND 5)\n               AND (((r.attempt) IS NOT NULL AND r.attempt BETWEEN 1 AND 1000020)\n    AND ((t.attempts) IS NOT NULL AND t.attempts BETWEEN 0 AND 1000000)\n    AND ((t.max_attempts) IS NOT NULL AND t.max_attempts BETWEEN 1 AND 1000000)\n    AND ((t.infra_retries) IS NOT NULL AND t.infra_retries BETWEEN 0 AND 20)\n    AND t.attempts < t.max_attempts\n    AND r.attempt = t.attempts + t.infra_retries + 1)\n               AND NOT EXISTS (\n    SELECT 1 FROM (SELECT higher_run.* FROM runs higher_run\n    WHERE higher_run.task_id = r.task_id) higher\n    WHERE (NOT ((higher.attempt) IS NOT NULL AND higher.attempt BETWEEN 1 AND 1000020)\n        OR higher.attempt > r.attempt)\n  ))\n        ORDER BY r.available_at_ms, r.run_id\n        LIMIT ?\n        FOR UPDATE SKIP LOCKED)\n             UNION ALL\n             (SELECT r.run_id, r.available_at_ms\n        FROM runs r FORCE INDEX (runs_poll)\n        WHERE r.queue = ? AND r.state = 'sleeping'\n          AND (((r.available_at_ms) IS NOT NULL AND r.available_at_ms BETWEEN 0 AND 253402300799000)\n    AND r.available_at_ms <= COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n))\n          AND EXISTS (SELECT 1 FROM tasks t\n                      WHERE t.task_id = r.task_id AND t.queue = r.queue AND t.state IN ('pending','running','sleeping') AND (t.cancel_at_ms IS NULL\n    OR (((t.cancel_at_ms) IS NOT NULL AND t.cancel_at_ms BETWEEN 0 AND 253402300799000)\n      AND t.cancel_at_ms > COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n)))\n               AND (\n    CASE\n      WHEN NOT JSON_VALID(t.retry_strategy) THEN 0\n      WHEN JSON_TYPE(CAST(t.retry_strategy AS JSON)) <> 'OBJECT' THEN 0\n      WHEN JSON_TYPE(JSON_EXTRACT(t.retry_strategy, '$.kind')) <> 'STRING' THEN 0\n      WHEN (JSON_UNQUOTE(JSON_EXTRACT(t.retry_strategy, '$.kind'))) = 'none' THEN 1\n      WHEN (JSON_UNQUOTE(JSON_EXTRACT(t.retry_strategy, '$.kind'))) = 'fixed' THEN CASE WHEN (CASE\n      WHEN JSON_TYPE(JSON_EXTRACT(t.retry_strategy, '$.baseSeconds')) NOT IN ('INTEGER','UNSIGNED INTEGER','DOUBLE','DECIMAL') THEN FALSE\n      WHEN CAST(JSON_EXTRACT(t.retry_strategy, '$.baseSeconds') AS DOUBLE) < 0 OR CAST(JSON_EXTRACT(t.retry_strategy, '$.baseSeconds') AS DOUBLE) > 3155760000000 THEN FALSE\n      ELSE ROUND(CAST(JSON_EXTRACT(t.retry_strategy, '$.baseSeconds') AS DOUBLE) * 1000) BETWEEN 0 AND 3155760000000\n    END) THEN 1 ELSE 0 END\n      WHEN (JSON_UNQUOTE(JSON_EXTRACT(t.retry_strategy, '$.kind'))) = 'exponential' THEN CASE\n        WHEN (CASE\n      WHEN JSON_TYPE(JSON_EXTRACT(t.retry_strategy, '$.baseSeconds')) NOT IN ('INTEGER','UNSIGNED INTEGER','DOUBLE','DECIMAL') THEN FALSE\n      WHEN CAST(JSON_EXTRACT(t.retry_strategy, '$.baseSeconds') AS DOUBLE) < 0 OR CAST(JSON_EXTRACT(t.retry_strategy, '$.baseSeconds') AS DOUBLE) > 3155760000000 THEN FALSE\n      ELSE ROUND(CAST(JSON_EXTRACT(t.retry_strategy, '$.baseSeconds') AS DOUBLE) * 1000) BETWEEN 0 AND 3155760000000\n    END)\n          AND (CASE\n      WHEN JSON_TYPE(JSON_EXTRACT(t.retry_strategy, '$.factor')) NOT IN ('INTEGER','UNSIGNED INTEGER','DOUBLE','DECIMAL') THEN FALSE\n      ELSE CAST(JSON_EXTRACT(t.retry_strategy, '$.factor') AS DOUBLE) BETWEEN 0 AND 1.7976931348623157e308\n    END)\n          AND (CASE\n      WHEN JSON_TYPE(JSON_EXTRACT(t.retry_strategy, '$.maxSeconds')) NOT IN ('INTEGER','UNSIGNED INTEGER','DOUBLE','DECIMAL') THEN FALSE\n      WHEN CAST(JSON_EXTRACT(t.retry_strategy, '$.maxSeconds') AS DOUBLE) < 0 OR CAST(JSON_EXTRACT(t.retry_strategy, '$.maxSeconds') AS DOUBLE) > 3155760000000 THEN FALSE\n      ELSE ROUND(CAST(JSON_EXTRACT(t.retry_strategy, '$.maxSeconds') AS DOUBLE) * 1000) BETWEEN 0 AND 3155760000000\n    END)\n        THEN 1 ELSE 0 END\n      ELSE 0\n    END = 1\n  )\n               AND (\n    CASE\n      WHEN t.headers IS NULL THEN 1\n      WHEN NOT JSON_VALID(t.headers) THEN 0\n      WHEN JSON_TYPE(CAST(t.headers AS JSON)) <> 'OBJECT' THEN 0\n      WHEN EXISTS (\n        SELECT 1 FROM JSON_TABLE(t.headers, '$.*' COLUMNS (member JSON PATH '$')) h\n        WHERE JSON_TYPE(h.member) <> 'STRING'\n      ) THEN 0\n      ELSE 1\n    END = 1\n  )\n               AND NOT EXISTS (SELECT 1 FROM (SELECT sibling_run.* FROM runs sibling_run\n    WHERE sibling_run.task_id = r.task_id) sibling\n               WHERE sibling.state IN ('pending','running','sleeping')\n                 AND sibling.run_id <> r.run_id)\n               AND (r.wake_step IS NOT NULL OR NOT EXISTS (SELECT 1 FROM waits w\n                          WHERE w.run_id = r.run_id\n      AND w.queue = r.queue\n      AND w.task_id = r.task_id\n      AND w.event_name = r.wake_event\n      AND w.status = 'waiting'\n      AND w.timeout_at_ms <=> r.available_at_ms\n      AND (r.available_at_ms IS NULL\n        OR ((r.available_at_ms) IS NOT NULL AND r.available_at_ms BETWEEN 0 AND 253402300799000))\n      AND (w.timeout_at_ms IS NULL\n        OR ((w.timeout_at_ms) IS NOT NULL AND w.timeout_at_ms BETWEEN 0 AND 253402300799000))\n                          GROUP BY w.run_id\n                              HAVING COUNT(*) > 1))\n               AND NOT EXISTS (\n      SELECT 1 FROM waits w\n      WHERE w.run_id = r.run_id AND w.status = 'waiting'\n        AND w.timeout_at_ms IS NOT NULL\n        AND NOT ((w.timeout_at_ms) IS NOT NULL AND w.timeout_at_ms BETWEEN 0 AND 253402300799000)\n    )\n               AND ((r.claim_gen) IS NOT NULL AND r.claim_gen BETWEEN 0 AND 999999)\n               AND ((r.activated_gen) IS NOT NULL AND r.activated_gen BETWEEN 0 AND 1000000)\n               AND r.activated_gen <= r.claim_gen\n               AND ((r.relaunch_count) IS NOT NULL AND r.relaunch_count BETWEEN 0 AND 5)\n               AND (((r.attempt) IS NOT NULL AND r.attempt BETWEEN 1 AND 1000020)\n    AND ((t.attempts) IS NOT NULL AND t.attempts BETWEEN 0 AND 1000000)\n    AND ((t.max_attempts) IS NOT NULL AND t.max_attempts BETWEEN 1 AND 1000000)\n    AND ((t.infra_retries) IS NOT NULL AND t.infra_retries BETWEEN 0 AND 20)\n    AND t.attempts < t.max_attempts\n    AND r.attempt = t.attempts + t.infra_retries + 1)\n               AND NOT EXISTS (\n    SELECT 1 FROM (SELECT higher_run.* FROM runs higher_run\n    WHERE higher_run.task_id = r.task_id) higher\n    WHERE (NOT ((higher.attempt) IS NOT NULL AND higher.attempt BETWEEN 1 AND 1000020)\n        OR higher.attempt > r.attempt)\n  ))\n        ORDER BY r.available_at_ms, r.run_id\n        LIMIT ?\n        FOR UPDATE SKIP LOCKED)\n           ) AS legs\n           ORDER BY legs.available_at_ms, legs.run_id\n           LIMIT ?\n         ) AS candidates\n       ) and not exists (select `held`.`run_id` from (select * from `runs`) as `held` where `held`.`queue` = ? and `held`.`state` = ? and `held`.`claimed_by` = ?) and (((COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n)) IS NOT NULL\n    AND (COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n)) BETWEEN 0 AND 253402300799000\n    AND (COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n)) <= 253402300799000 - (CAST((?) AS SIGNED))))",
        "bindArity": 15
      },
      {
        "sql": "update `tasks` set `state` = ('running'), `last_attempt_run` = ((SELECT MIN(f.run_id)\n           FROM runs f\n           WHERE f.task_id = tasks.task_id AND f.fence_stamp = ?\n           GROUP BY f.task_id\n            HAVING COUNT(*) = 1)), `fence_stamp` = ?, `fence_at_ms` = (select min(`f`.`fence_at_ms`) as `source_instant` from `runs` as `f` where (f.queue = ? AND f.state = 'running') and `f`.`queue` = `tasks`.`queue` and `f`.`fence_stamp` = ?) where `task_id` in (select `f`.`task_id` from `runs` as `f` where (f.queue = ? AND f.state = 'running') and `f`.`queue` = `tasks`.`queue` and `f`.`fence_stamp` = ?) and (state IN ('pending','running','sleeping'))",
        "bindArity": 6
      },
      {
        "sql": "delete from `waits` where `run_id` in (select `f`.`run_id` from `runs` as `f` where (f.queue = ? AND f.state = 'running') and `f`.`fence_stamp` = ?) and (status = 'waiting'\n            AND ((timeout_at_ms) IS NOT NULL AND timeout_at_ms BETWEEN 0 AND 253402300799000)\n            AND timeout_at_ms <= (SELECT f.fence_at_ms FROM runs f WHERE f.run_id = waits.run_id AND f.fence_stamp = ?))",
        "bindArity": 3
      },
      {
        "sql": "select `r`.`run_id`, `r`.`task_id`, `r`.`attempt`, `r`.`claim_gen`, `r`.`claim_expires_at_ms`, `r`.`lease_ms`, `r`.`wake_event`, `r`.`event_payload`, `r`.`wake_step`, `t`.`task_name`, `t`.`params`, `t`.`retry_strategy`, `t`.`max_attempts`, `t`.`headers`, `t`.`infra_retries` from `runs` as `r` inner join `tasks` as `t` on (t.task_id = r.task_id AND t.queue = r.queue) where `r`.`queue` = ? and `r`.`claimed_by` = ? and `r`.`state` = ? and (t.state IN ('pending','running','sleeping')\n         AND (\n    CASE\n      WHEN NOT JSON_VALID(t.retry_strategy) THEN 0\n      WHEN JSON_TYPE(CAST(t.retry_strategy AS JSON)) <> 'OBJECT' THEN 0\n      WHEN JSON_TYPE(JSON_EXTRACT(t.retry_strategy, '$.kind')) <> 'STRING' THEN 0\n      WHEN (JSON_UNQUOTE(JSON_EXTRACT(t.retry_strategy, '$.kind'))) = 'none' THEN 1\n      WHEN (JSON_UNQUOTE(JSON_EXTRACT(t.retry_strategy, '$.kind'))) = 'fixed' THEN CASE WHEN (CASE\n      WHEN JSON_TYPE(JSON_EXTRACT(t.retry_strategy, '$.baseSeconds')) NOT IN ('INTEGER','UNSIGNED INTEGER','DOUBLE','DECIMAL') THEN FALSE\n      WHEN CAST(JSON_EXTRACT(t.retry_strategy, '$.baseSeconds') AS DOUBLE) < 0 OR CAST(JSON_EXTRACT(t.retry_strategy, '$.baseSeconds') AS DOUBLE) > 3155760000000 THEN FALSE\n      ELSE ROUND(CAST(JSON_EXTRACT(t.retry_strategy, '$.baseSeconds') AS DOUBLE) * 1000) BETWEEN 0 AND 3155760000000\n    END) THEN 1 ELSE 0 END\n      WHEN (JSON_UNQUOTE(JSON_EXTRACT(t.retry_strategy, '$.kind'))) = 'exponential' THEN CASE\n        WHEN (CASE\n      WHEN JSON_TYPE(JSON_EXTRACT(t.retry_strategy, '$.baseSeconds')) NOT IN ('INTEGER','UNSIGNED INTEGER','DOUBLE','DECIMAL') THEN FALSE\n      WHEN CAST(JSON_EXTRACT(t.retry_strategy, '$.baseSeconds') AS DOUBLE) < 0 OR CAST(JSON_EXTRACT(t.retry_strategy, '$.baseSeconds') AS DOUBLE) > 3155760000000 THEN FALSE\n      ELSE ROUND(CAST(JSON_EXTRACT(t.retry_strategy, '$.baseSeconds') AS DOUBLE) * 1000) BETWEEN 0 AND 3155760000000\n    END)\n          AND (CASE\n      WHEN JSON_TYPE(JSON_EXTRACT(t.retry_strategy, '$.factor')) NOT IN ('INTEGER','UNSIGNED INTEGER','DOUBLE','DECIMAL') THEN FALSE\n      ELSE CAST(JSON_EXTRACT(t.retry_strategy, '$.factor') AS DOUBLE) BETWEEN 0 AND 1.7976931348623157e308\n    END)\n          AND (CASE\n      WHEN JSON_TYPE(JSON_EXTRACT(t.retry_strategy, '$.maxSeconds')) NOT IN ('INTEGER','UNSIGNED INTEGER','DOUBLE','DECIMAL') THEN FALSE\n      WHEN CAST(JSON_EXTRACT(t.retry_strategy, '$.maxSeconds') AS DOUBLE) < 0 OR CAST(JSON_EXTRACT(t.retry_strategy, '$.maxSeconds') AS DOUBLE) > 3155760000000 THEN FALSE\n      ELSE ROUND(CAST(JSON_EXTRACT(t.retry_strategy, '$.maxSeconds') AS DOUBLE) * 1000) BETWEEN 0 AND 3155760000000\n    END)\n        THEN 1 ELSE 0 END\n      ELSE 0\n    END = 1\n  )\n         AND (\n    CASE\n      WHEN t.headers IS NULL THEN 1\n      WHEN NOT JSON_VALID(t.headers) THEN 0\n      WHEN JSON_TYPE(CAST(t.headers AS JSON)) <> 'OBJECT' THEN 0\n      WHEN EXISTS (\n        SELECT 1 FROM JSON_TABLE(t.headers, '$.*' COLUMNS (member JSON PATH '$')) h\n        WHERE JSON_TYPE(h.member) <> 'STRING'\n      ) THEN 0\n      ELSE 1\n    END = 1\n  )\n         AND NOT EXISTS (SELECT 1 FROM (SELECT sibling_run.* FROM runs sibling_run\n    WHERE sibling_run.task_id = r.task_id) sibling\n               WHERE sibling.state IN ('pending','running','sleeping')\n                 AND sibling.run_id <> r.run_id)\n         AND ((r.claim_gen) IS NOT NULL AND r.claim_gen BETWEEN 1 AND 1000000)\n         AND ((r.activated_gen) IS NOT NULL AND r.activated_gen BETWEEN 0 AND 1000000)\n         AND r.activated_gen <= r.claim_gen\n         AND ((r.relaunch_count) IS NOT NULL AND r.relaunch_count BETWEEN 0 AND 5)\n         AND ((r.lease_ms) IS NOT NULL AND r.lease_ms BETWEEN 1 AND 3155760000000)\n         AND ((r.claim_expires_at_ms) IS NOT NULL AND r.claim_expires_at_ms BETWEEN 0 AND 253402300799000)\n         AND (((r.attempt) IS NOT NULL AND r.attempt BETWEEN 1 AND 1000020)\n    AND ((t.attempts) IS NOT NULL AND t.attempts BETWEEN 0 AND 1000000)\n    AND ((t.max_attempts) IS NOT NULL AND t.max_attempts BETWEEN 1 AND 1000000)\n    AND ((t.infra_retries) IS NOT NULL AND t.infra_retries BETWEEN 0 AND 20)\n    AND t.attempts < t.max_attempts\n    AND r.attempt = t.attempts + t.infra_retries + 1)\n         AND NOT EXISTS (\n    SELECT 1 FROM (SELECT higher_run.* FROM runs higher_run\n    WHERE higher_run.task_id = r.task_id) higher\n    WHERE (NOT ((higher.attempt) IS NOT NULL AND higher.attempt BETWEEN 1 AND 1000020)\n        OR higher.attempt > r.attempt)\n  )) order by `r`.`run_id`",
        "bindArity": 3
      }
    ]
  },
  "activate": {
    "activated": [
      {
        "sql": "update `runs` set `activated_gen` = ?, `started_at_ms` = coalesce(`started_at_ms`, COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n)), `claim_expires_at_ms` = (COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n) + lease_ms), `heartbeat_at_ms` = COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n), `fence_stamp` = ?, `fence_at_ms` = COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n) where `run_id` = ? and `queue` = ? and `claimed_by` = ? and `state` = ? and `claim_gen` = ? and `activated_gen` < ? and ((((runs.claim_gen) IS NOT NULL AND runs.claim_gen BETWEEN 1 AND 1000000)\n    AND ((runs.activated_gen) IS NOT NULL AND runs.activated_gen BETWEEN 0 AND 1000000)\n    AND ((runs.lease_ms) IS NOT NULL AND runs.lease_ms BETWEEN 1 AND 3155760000000)\n    AND ((runs.relaunch_count) IS NOT NULL AND runs.relaunch_count BETWEEN 0 AND 5)\n    AND NOT EXISTS (SELECT 1 FROM (SELECT sibling_run.* FROM runs sibling_run\n    WHERE sibling_run.task_id = runs.task_id) sibling\n               WHERE sibling.state IN ('pending','running','sleeping')\n                 AND sibling.run_id <> runs.run_id)\n    AND EXISTS (\n      SELECT 1 FROM tasks t\n      WHERE t.task_id = runs.task_id AND t.queue = runs.queue AND t.state IN ('pending','running','sleeping') AND (t.cancel_at_ms IS NULL\n    OR (((t.cancel_at_ms) IS NOT NULL AND t.cancel_at_ms BETWEEN 0 AND 253402300799000)\n      AND t.cancel_at_ms > COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n)))\n        AND (\n    CASE\n      WHEN NOT JSON_VALID(t.retry_strategy) THEN 0\n      WHEN JSON_TYPE(CAST(t.retry_strategy AS JSON)) <> 'OBJECT' THEN 0\n      WHEN JSON_TYPE(JSON_EXTRACT(t.retry_strategy, '$.kind')) <> 'STRING' THEN 0\n      WHEN (JSON_UNQUOTE(JSON_EXTRACT(t.retry_strategy, '$.kind'))) = 'none' THEN 1\n      WHEN (JSON_UNQUOTE(JSON_EXTRACT(t.retry_strategy, '$.kind'))) = 'fixed' THEN CASE WHEN (CASE\n      WHEN JSON_TYPE(JSON_EXTRACT(t.retry_strategy, '$.baseSeconds')) NOT IN ('INTEGER','UNSIGNED INTEGER','DOUBLE','DECIMAL') THEN FALSE\n      WHEN CAST(JSON_EXTRACT(t.retry_strategy, '$.baseSeconds') AS DOUBLE) < 0 OR CAST(JSON_EXTRACT(t.retry_strategy, '$.baseSeconds') AS DOUBLE) > 3155760000000 THEN FALSE\n      ELSE ROUND(CAST(JSON_EXTRACT(t.retry_strategy, '$.baseSeconds') AS DOUBLE) * 1000) BETWEEN 0 AND 3155760000000\n    END) THEN 1 ELSE 0 END\n      WHEN (JSON_UNQUOTE(JSON_EXTRACT(t.retry_strategy, '$.kind'))) = 'exponential' THEN CASE\n        WHEN (CASE\n      WHEN JSON_TYPE(JSON_EXTRACT(t.retry_strategy, '$.baseSeconds')) NOT IN ('INTEGER','UNSIGNED INTEGER','DOUBLE','DECIMAL') THEN FALSE\n      WHEN CAST(JSON_EXTRACT(t.retry_strategy, '$.baseSeconds') AS DOUBLE) < 0 OR CAST(JSON_EXTRACT(t.retry_strategy, '$.baseSeconds') AS DOUBLE) > 3155760000000 THEN FALSE\n      ELSE ROUND(CAST(JSON_EXTRACT(t.retry_strategy, '$.baseSeconds') AS DOUBLE) * 1000) BETWEEN 0 AND 3155760000000\n    END)\n          AND (CASE\n      WHEN JSON_TYPE(JSON_EXTRACT(t.retry_strategy, '$.factor')) NOT IN ('INTEGER','UNSIGNED INTEGER','DOUBLE','DECIMAL') THEN FALSE\n      ELSE CAST(JSON_EXTRACT(t.retry_strategy, '$.factor') AS DOUBLE) BETWEEN 0 AND 1.7976931348623157e308\n    END)\n          AND (CASE\n      WHEN JSON_TYPE(JSON_EXTRACT(t.retry_strategy, '$.maxSeconds')) NOT IN ('INTEGER','UNSIGNED INTEGER','DOUBLE','DECIMAL') THEN FALSE\n      WHEN CAST(JSON_EXTRACT(t.retry_strategy, '$.maxSeconds') AS DOUBLE) < 0 OR CAST(JSON_EXTRACT(t.retry_strategy, '$.maxSeconds') AS DOUBLE) > 3155760000000 THEN FALSE\n      ELSE ROUND(CAST(JSON_EXTRACT(t.retry_strategy, '$.maxSeconds') AS DOUBLE) * 1000) BETWEEN 0 AND 3155760000000\n    END)\n        THEN 1 ELSE 0 END\n      ELSE 0\n    END = 1\n  )\n        AND (\n    CASE\n      WHEN t.headers IS NULL THEN 1\n      WHEN NOT JSON_VALID(t.headers) THEN 0\n      WHEN JSON_TYPE(CAST(t.headers AS JSON)) <> 'OBJECT' THEN 0\n      WHEN EXISTS (\n        SELECT 1 FROM JSON_TABLE(t.headers, '$.*' COLUMNS (member JSON PATH '$')) h\n        WHERE JSON_TYPE(h.member) <> 'STRING'\n      ) THEN 0\n      ELSE 1\n    END = 1\n  )\n        AND (((runs.attempt) IS NOT NULL AND runs.attempt BETWEEN 1 AND 1000020)\n    AND ((t.attempts) IS NOT NULL AND t.attempts BETWEEN 0 AND 1000000)\n    AND ((t.max_attempts) IS NOT NULL AND t.max_attempts BETWEEN 1 AND 1000000)\n    AND ((t.infra_retries) IS NOT NULL AND t.infra_retries BETWEEN 0 AND 20)\n    AND t.attempts < t.max_attempts\n    AND runs.attempt = t.attempts + t.infra_retries + 1)\n        AND NOT EXISTS (\n    SELECT 1 FROM (SELECT higher_run.* FROM runs higher_run\n    WHERE higher_run.task_id = runs.task_id) higher\n    WHERE (NOT ((higher.attempt) IS NOT NULL AND higher.attempt BETWEEN 1 AND 1000020)\n        OR higher.attempt > runs.attempt)\n  )\n        AND (CASE\n    WHEN t.cancellation IS NULL THEN 1\n    WHEN NOT JSON_VALID(t.cancellation) THEN 0\n    WHEN JSON_TYPE(CAST(t.cancellation AS JSON)) <> 'OBJECT' THEN 0\n    WHEN JSON_EXTRACT(t.cancellation, '$.maxDurationSeconds') IS NULL THEN 1\n    WHEN JSON_TYPE(JSON_EXTRACT(t.cancellation, '$.maxDurationSeconds')) NOT IN ('INTEGER','UNSIGNED INTEGER','DOUBLE','DECIMAL') THEN 0\n    WHEN (CAST(JSON_EXTRACT(t.cancellation, '$.maxDurationSeconds') AS DOUBLE)) < 0 OR (CAST(JSON_EXTRACT(t.cancellation, '$.maxDurationSeconds') AS DOUBLE)) > 3155760000000 THEN 0\n    WHEN (CAST(ROUND(CAST(JSON_EXTRACT(t.cancellation, '$.maxDurationSeconds') AS DOUBLE) * 1000) AS SIGNED)) > 3155760000000 THEN 0\n    WHEN NOT ((COALESCE(t.first_started_at_ms, COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n))) IS NOT NULL\n    AND (COALESCE(t.first_started_at_ms, COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n))) BETWEEN 0 AND 253402300799000\n    AND (COALESCE(t.first_started_at_ms, COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n))) <= 253402300799000 - (CAST((CAST(ROUND(CAST(JSON_EXTRACT(t.cancellation, '$.maxDurationSeconds') AS DOUBLE) * 1000) AS SIGNED)) AS SIGNED))) THEN 0\n    ELSE 1\n  END = 1)\n    ))) and (((COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n)) IS NOT NULL\n    AND (COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n)) BETWEEN 0 AND 253402300799000\n    AND (COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n)) <= 253402300799000 - (CAST((runs.lease_ms) AS SIGNED))))",
        "bindArity": 8
      },
      {
        "sql": "update `tasks` set `cancel_at_ms` = (CASE\n          WHEN JSON_EXTRACT(tasks.cancellation, '$.maxDurationSeconds') IS NOT NULL THEN\n            COALESCE(first_started_at_ms, (SELECT f.fence_at_ms FROM runs f WHERE f.run_id = ? AND f.fence_stamp = ?)) + CAST(ROUND(CAST(JSON_EXTRACT(tasks.cancellation, '$.maxDurationSeconds') AS DOUBLE) * 1000) AS SIGNED)\n          ELSE NULL\n        END), `first_started_at_ms` = coalesce(`first_started_at_ms`, ((SELECT f.fence_at_ms FROM runs f WHERE f.run_id = ? AND f.fence_stamp = ?))), `fence_stamp` = ?, `fence_at_ms` = (select min(`f`.`fence_at_ms`) as `source_instant` from `runs` as `f` where (f.run_id = ?) and `f`.`queue` = `tasks`.`queue` and `f`.`fence_stamp` = ?) where `task_id` in (select `f`.`task_id` from `runs` as `f` where (f.run_id = ?) and `f`.`queue` = `tasks`.`queue` and `f`.`fence_stamp` = ?) and (state IN ('pending','running','sleeping'))",
        "bindArity": 9
      },
      {
        "sql": "select `r`.`run_id`, `r`.`task_id`, `r`.`attempt`, `r`.`claim_gen`, `r`.`claim_expires_at_ms`, `r`.`lease_ms`, `r`.`wake_event`, `r`.`event_payload`, `r`.`wake_step`, `t`.`task_name`, `t`.`params`, `t`.`retry_strategy`, `t`.`max_attempts`, `t`.`headers`, `t`.`infra_retries` from `runs` as `r` inner join `tasks` as `t` on (t.task_id = r.task_id AND t.queue = r.queue) where `r`.`run_id` = ? and `r`.`fence_stamp` = ? and `r`.`state` = ?",
        "bindArity": 3
      }
    ]
  },
  "complete": {
    "completed": [
      {
        "sql": "update `runs` set `state` = ?, `completed_at_ms` = COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n), `result` = ?, `wake_event` = ?, `event_payload` = ?, `wake_step` = ?, `claimed_by` = ?, `claim_expires_at_ms` = ?, `fence_stamp` = ?, `fence_at_ms` = COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n) where `run_id` = ? and `queue` = ? and `claimed_by` = ? and `state` = ? and (EXISTS (\n  SELECT 1 FROM tasks t\n  WHERE t.task_id = runs.task_id AND t.queue = runs.queue\n    AND (t.state NOT IN ('pending','running','sleeping')\n      OR (t.state IN ('pending','running','sleeping') AND NOT EXISTS (SELECT 1 FROM (SELECT sibling_run.* FROM runs sibling_run\n    WHERE sibling_run.task_id = runs.task_id) sibling\n               WHERE sibling.state IN ('pending','running','sleeping')\n                 AND sibling.run_id <> runs.run_id)))\n))",
        "bindArity": 12
      },
      {
        "sql": "update `tasks` set `state` = ('completed'), `completed_payload` = (?), `cancel_at_ms` = (NULL), `fence_stamp` = ?, `fence_at_ms` = (select min(`f`.`fence_at_ms`) as `source_instant` from `runs` as `f` where (f.run_id = ?) and `f`.`queue` = `tasks`.`queue` and `f`.`fence_stamp` = ?) where `task_id` in (select `f`.`task_id` from `runs` as `f` where (f.run_id = ?) and `f`.`queue` = `tasks`.`queue` and `f`.`fence_stamp` = ?) and (state IN ('pending','running','sleeping'))",
        "bindArity": 6
      },
      {
        "sql": "delete from `waits` where `run_id` in (select `f`.`run_id` from `runs` as `f` where (f.run_id = ?) and `f`.`fence_stamp` = ?)",
        "bindArity": 2
      },
      {
        "sql": "insert into `events` (`queue`, `event_name`, `payload`, `emitted_at_ms`, `fence_stamp`, `fence_at_ms`) select `f`.`queue` as `queue`, ? as `event_name`, ? as `payload`, `f`.`fence_at_ms` as `emitted_at_ms`, ? as `fence_stamp`, `f`.`fence_at_ms` as `fence_at_ms` from `tasks` as `f` where `f`.`task_id` = ? and `f`.`queue` = ? and `f`.`fence_stamp` = ? and not exists (select `e`.`queue` from `events` as `e` where `e`.`queue` = `f`.`queue` and `e`.`event_name` = ?)",
        "bindArity": 7
      },
      {
        "sql": "update `runs` set `state` = ?, `wake_step` = coalesce(`wake_step`, ((SELECT MIN(w.step_name)\n           FROM waits w\n           WHERE w.run_id = runs.run_id\n      AND w.queue = runs.queue\n      AND w.task_id = runs.task_id\n      AND w.event_name = runs.wake_event\n      AND w.status = 'waiting'\n      AND w.timeout_at_ms <=> runs.available_at_ms\n      AND (runs.available_at_ms IS NULL\n        OR ((runs.available_at_ms) IS NOT NULL AND runs.available_at_ms BETWEEN 0 AND 253402300799000))\n      AND (w.timeout_at_ms IS NULL\n        OR ((w.timeout_at_ms) IS NOT NULL AND w.timeout_at_ms BETWEEN 0 AND 253402300799000))\n           GROUP BY w.run_id\n            HAVING COUNT(*) = 1))), `available_at_ms` = (select `f`.`fence_at_ms` from `events` as `f` where `f`.`queue` = `runs`.`queue` and `f`.`event_name` = ? and `f`.`fence_stamp` = ?), `wake_event` = ?, `event_payload` = (select `f`.`payload` from `events` as `f` where `f`.`queue` = `runs`.`queue` and `f`.`event_name` = ? and `f`.`fence_stamp` = ?), `fence_stamp` = ?, `fence_at_ms` = (select `f`.`fence_at_ms` from `events` as `f` where `f`.`queue` = `runs`.`queue` and `f`.`event_name` = ? and `f`.`fence_stamp` = ?) where `state` = ? and (wake_event = ?) and `run_id` in (SELECT w.run_id FROM waits w\n                        WHERE w.queue = ? AND w.event_name = ? AND w.status = 'waiting') and ((runs.wake_step IS NOT NULL AND EXISTS (SELECT 1 FROM waits w\n                      WHERE w.run_id = runs.run_id\n      AND w.queue = runs.queue\n      AND w.task_id = runs.task_id\n      AND w.event_name = runs.wake_event\n      AND w.status = 'waiting'\n      AND w.timeout_at_ms <=> runs.available_at_ms\n      AND (runs.available_at_ms IS NULL\n        OR ((runs.available_at_ms) IS NOT NULL AND runs.available_at_ms BETWEEN 0 AND 253402300799000))\n      AND (w.timeout_at_ms IS NULL\n        OR ((w.timeout_at_ms) IS NOT NULL AND w.timeout_at_ms BETWEEN 0 AND 253402300799000))\n                        AND w.step_name = runs.wake_step))\n              OR (runs.wake_step IS NULL AND (SELECT MIN(w.step_name)\n           FROM waits w\n           WHERE w.run_id = runs.run_id\n      AND w.queue = runs.queue\n      AND w.task_id = runs.task_id\n      AND w.event_name = runs.wake_event\n      AND w.status = 'waiting'\n      AND w.timeout_at_ms <=> runs.available_at_ms\n      AND (runs.available_at_ms IS NULL\n        OR ((runs.available_at_ms) IS NOT NULL AND runs.available_at_ms BETWEEN 0 AND 253402300799000))\n      AND (w.timeout_at_ms IS NULL\n        OR ((w.timeout_at_ms) IS NOT NULL AND w.timeout_at_ms BETWEEN 0 AND 253402300799000))\n           GROUP BY w.run_id\n            HAVING COUNT(*) = 1) IS NOT NULL)) and exists (select `f`.`queue` from `events` as `f` where `f`.`queue` = `runs`.`queue` and `f`.`event_name` = ? and `f`.`fence_stamp` = ?) and (EXISTS (SELECT 1 FROM tasks t\n                     WHERE t.task_id = runs.task_id AND t.queue = runs.queue AND t.state IN ('pending','running','sleeping')))",
        "bindArity": 15
      },
      {
        "sql": "update `tasks` set `state` = ('pending'), `fence_stamp` = ?, `fence_at_ms` = (select min(`f`.`fence_at_ms`) as `source_instant` from `runs` as `f` where (f.wake_event = ? AND f.state = 'pending') and `f`.`queue` = ? and `f`.`fence_stamp` = ?) where `task_id` in (select `f`.`task_id` from `runs` as `f` where (f.wake_event = ? AND f.state = 'pending') and `f`.`queue` = ? and `f`.`fence_stamp` = ?) and `tasks`.`queue` = ? and (state IN ('pending','running','sleeping'))",
        "bindArity": 8
      },
      {
        "sql": "delete from `waits` where `run_id` in (select `f`.`run_id` from `runs` as `f` where (f.queue = ? AND f.wake_event = ? AND f.state = 'pending') and `f`.`fence_stamp` = ?) and (event_name = ? AND status = 'waiting')",
        "bindArity": 4
      },
      {
        "sql": "update `runs` set `run_id` = `run_id`, `fence_stamp` = ?, `fence_at_ms` = (select min(`source_fence_at_ms`) as `source_instant` from (select distinct `f`.`fence_at_ms` as `source_fence_at_ms` from `runs` as `f` where (f.queue = ? AND f.wake_event = ? AND f.state = 'pending') and `f`.`fence_stamp` = ?) as `fenced_source_instant`) where `run_id` in (select `source_key` from (select distinct `f`.`run_id` as `source_key` from `runs` as `f` where (f.queue = ? AND f.wake_event = ? AND f.state = 'pending') and `f`.`fence_stamp` = ?) as `fenced_source`)",
        "bindArity": 7
      }
    ]
  },
  "defer-launch": {
    "deferred": [
      {
        "sql": "update `runs` set `state` = case when ((CASE WHEN ? = 1 THEN COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n) + ? ELSE ? END)) <= COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n) then ? else ? end, `available_at_ms` = ((CASE WHEN ? = 1 THEN COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n) + ? ELSE ? END)), `claimed_by` = ?, `claim_expires_at_ms` = ?, `heartbeat_at_ms` = ?, `fence_stamp` = ?, `fence_at_ms` = COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n) where `run_id` = ? and `queue` = ? and `claimed_by` = ? and `state` = ? and `claim_gen` = ? and `activated_gen` < ? and ((((runs.claim_gen) IS NOT NULL AND runs.claim_gen BETWEEN 1 AND 1000000)\n    AND ((runs.activated_gen) IS NOT NULL AND runs.activated_gen BETWEEN 0 AND 1000000)\n    AND ((runs.lease_ms) IS NOT NULL AND runs.lease_ms BETWEEN 1 AND 3155760000000)\n    AND ((runs.relaunch_count) IS NOT NULL AND runs.relaunch_count BETWEEN 0 AND 5)\n    AND NOT EXISTS (SELECT 1 FROM (SELECT sibling_run.* FROM runs sibling_run\n    WHERE sibling_run.task_id = runs.task_id) sibling\n               WHERE sibling.state IN ('pending','running','sleeping')\n                 AND sibling.run_id <> runs.run_id)\n    AND EXISTS (\n      SELECT 1 FROM tasks t\n      WHERE t.task_id = runs.task_id AND t.queue = runs.queue AND t.state IN ('pending','running','sleeping') AND (t.cancel_at_ms IS NULL\n    OR (((t.cancel_at_ms) IS NOT NULL AND t.cancel_at_ms BETWEEN 0 AND 253402300799000)\n      AND t.cancel_at_ms > COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n)))\n        AND (\n    CASE\n      WHEN NOT JSON_VALID(t.retry_strategy) THEN 0\n      WHEN JSON_TYPE(CAST(t.retry_strategy AS JSON)) <> 'OBJECT' THEN 0\n      WHEN JSON_TYPE(JSON_EXTRACT(t.retry_strategy, '$.kind')) <> 'STRING' THEN 0\n      WHEN (JSON_UNQUOTE(JSON_EXTRACT(t.retry_strategy, '$.kind'))) = 'none' THEN 1\n      WHEN (JSON_UNQUOTE(JSON_EXTRACT(t.retry_strategy, '$.kind'))) = 'fixed' THEN CASE WHEN (CASE\n      WHEN JSON_TYPE(JSON_EXTRACT(t.retry_strategy, '$.baseSeconds')) NOT IN ('INTEGER','UNSIGNED INTEGER','DOUBLE','DECIMAL') THEN FALSE\n      WHEN CAST(JSON_EXTRACT(t.retry_strategy, '$.baseSeconds') AS DOUBLE) < 0 OR CAST(JSON_EXTRACT(t.retry_strategy, '$.baseSeconds') AS DOUBLE) > 3155760000000 THEN FALSE\n      ELSE ROUND(CAST(JSON_EXTRACT(t.retry_strategy, '$.baseSeconds') AS DOUBLE) * 1000) BETWEEN 0 AND 3155760000000\n    END) THEN 1 ELSE 0 END\n      WHEN (JSON_UNQUOTE(JSON_EXTRACT(t.retry_strategy, '$.kind'))) = 'exponential' THEN CASE\n        WHEN (CASE\n      WHEN JSON_TYPE(JSON_EXTRACT(t.retry_strategy, '$.baseSeconds')) NOT IN ('INTEGER','UNSIGNED INTEGER','DOUBLE','DECIMAL') THEN FALSE\n      WHEN CAST(JSON_EXTRACT(t.retry_strategy, '$.baseSeconds') AS DOUBLE) < 0 OR CAST(JSON_EXTRACT(t.retry_strategy, '$.baseSeconds') AS DOUBLE) > 3155760000000 THEN FALSE\n      ELSE ROUND(CAST(JSON_EXTRACT(t.retry_strategy, '$.baseSeconds') AS DOUBLE) * 1000) BETWEEN 0 AND 3155760000000\n    END)\n          AND (CASE\n      WHEN JSON_TYPE(JSON_EXTRACT(t.retry_strategy, '$.factor')) NOT IN ('INTEGER','UNSIGNED INTEGER','DOUBLE','DECIMAL') THEN FALSE\n      ELSE CAST(JSON_EXTRACT(t.retry_strategy, '$.factor') AS DOUBLE) BETWEEN 0 AND 1.7976931348623157e308\n    END)\n          AND (CASE\n      WHEN JSON_TYPE(JSON_EXTRACT(t.retry_strategy, '$.maxSeconds')) NOT IN ('INTEGER','UNSIGNED INTEGER','DOUBLE','DECIMAL') THEN FALSE\n      WHEN CAST(JSON_EXTRACT(t.retry_strategy, '$.maxSeconds') AS DOUBLE) < 0 OR CAST(JSON_EXTRACT(t.retry_strategy, '$.maxSeconds') AS DOUBLE) > 3155760000000 THEN FALSE\n      ELSE ROUND(CAST(JSON_EXTRACT(t.retry_strategy, '$.maxSeconds') AS DOUBLE) * 1000) BETWEEN 0 AND 3155760000000\n    END)\n        THEN 1 ELSE 0 END\n      ELSE 0\n    END = 1\n  )\n        AND (\n    CASE\n      WHEN t.headers IS NULL THEN 1\n      WHEN NOT JSON_VALID(t.headers) THEN 0\n      WHEN JSON_TYPE(CAST(t.headers AS JSON)) <> 'OBJECT' THEN 0\n      WHEN EXISTS (\n        SELECT 1 FROM JSON_TABLE(t.headers, '$.*' COLUMNS (member JSON PATH '$')) h\n        WHERE JSON_TYPE(h.member) <> 'STRING'\n      ) THEN 0\n      ELSE 1\n    END = 1\n  )\n        AND (((runs.attempt) IS NOT NULL AND runs.attempt BETWEEN 1 AND 1000020)\n    AND ((t.attempts) IS NOT NULL AND t.attempts BETWEEN 0 AND 1000000)\n    AND ((t.max_attempts) IS NOT NULL AND t.max_attempts BETWEEN 1 AND 1000000)\n    AND ((t.infra_retries) IS NOT NULL AND t.infra_retries BETWEEN 0 AND 20)\n    AND t.attempts < t.max_attempts\n    AND runs.attempt = t.attempts + t.infra_retries + 1)\n        AND NOT EXISTS (\n    SELECT 1 FROM (SELECT higher_run.* FROM runs higher_run\n    WHERE higher_run.task_id = runs.task_id) higher\n    WHERE (NOT ((higher.attempt) IS NOT NULL AND higher.attempt BETWEEN 1 AND 1000020)\n        OR higher.attempt > runs.attempt)\n  )\n    ))) and ((CASE WHEN ? = 1 THEN ((COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n)) IS NOT NULL\n    AND (COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n)) BETWEEN 0 AND 253402300799000\n    AND (COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n)) <= 253402300799000 - (CAST((?) AS SIGNED))) ELSE TRUE END))",
        "bindArity": 20
      },
      {
        "sql": "update `tasks` set `state` = ((SELECT f.state FROM runs f\n               WHERE f.run_id = ? AND f.fence_stamp = ?)), `fence_stamp` = ?, `fence_at_ms` = (select min(`f`.`fence_at_ms`) as `source_instant` from `runs` as `f` where (f.run_id = ?) and `f`.`queue` = `tasks`.`queue` and `f`.`fence_stamp` = ?) where `task_id` in (select `f`.`task_id` from `runs` as `f` where (f.run_id = ?) and `f`.`queue` = `tasks`.`queue` and `f`.`fence_stamp` = ?) and (state IN ('pending','running','sleeping'))",
        "bindArity": 7
      },
      {
        "sql": "delete from `waits` where `run_id` in (select `f`.`run_id` from `runs` as `f` where (f.run_id = ?) and `f`.`fence_stamp` = ?)",
        "bindArity": 2
      }
    ]
  },
  "reschedule": {
    "rescheduled": [
      {
        "sql": "update `runs` set `state` = case when ((CASE WHEN ? = 1 THEN COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n) + ? ELSE ? END)) <= COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n) then ? else ? end, `available_at_ms` = ((CASE WHEN ? = 1 THEN COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n) + ? ELSE ? END)), `claimed_by` = ?, `claim_expires_at_ms` = ?, `heartbeat_at_ms` = ?, `fence_stamp` = ?, `fence_at_ms` = COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n), `wake_event` = ?, `event_payload` = ?, `wake_step` = ? where `run_id` = ? and `queue` = ? and `claimed_by` = ? and `state` = ? and ((runs.attempt) IS NOT NULL\n         AND EXISTS (SELECT 1 FROM tasks t\n                     WHERE t.task_id = runs.task_id AND t.queue = runs.queue AND t.state IN ('pending','running','sleeping') AND (t.cancel_at_ms IS NULL\n    OR (((t.cancel_at_ms) IS NOT NULL AND t.cancel_at_ms BETWEEN 0 AND 253402300799000)\n      AND t.cancel_at_ms > COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n))))) and ((CASE WHEN ? = 1 THEN ((COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n)) IS NOT NULL\n    AND (COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n)) BETWEEN 0 AND 253402300799000\n    AND (COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n)) <= 253402300799000 - (CAST((?) AS SIGNED))) ELSE TRUE END))",
        "bindArity": 21
      },
      {
        "sql": "update `tasks` set `state` = ((SELECT f.state FROM runs f\n               WHERE f.run_id = ? AND f.fence_stamp = ?)), `fence_stamp` = ?, `fence_at_ms` = (select min(`f`.`fence_at_ms`) as `source_instant` from `runs` as `f` where (f.run_id = ?) and `f`.`queue` = `tasks`.`queue` and `f`.`fence_stamp` = ?) where `task_id` in (select `f`.`task_id` from `runs` as `f` where (f.run_id = ?) and `f`.`queue` = `tasks`.`queue` and `f`.`fence_stamp` = ?) and (state IN ('pending','running','sleeping'))",
        "bindArity": 7
      },
      {
        "sql": "delete from `waits` where `run_id` in (select `f`.`run_id` from `runs` as `f` where (f.run_id = ?) and `f`.`fence_stamp` = ?)",
        "bindArity": 2
      }
    ]
  },
  "suspend": {
    "suspended": [
      {
        "sql": "update `runs` set `state` = case when ((CASE WHEN ? = 1 THEN COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n) + ? ELSE ? END)) <= COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n) then ? else ? end, `available_at_ms` = ((CASE WHEN ? = 1 THEN COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n) + ? ELSE ? END)), `claimed_by` = ?, `claim_expires_at_ms` = ?, `heartbeat_at_ms` = ?, `fence_stamp` = ?, `fence_at_ms` = COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n), `wake_event` = ?, `event_payload` = ?, `wake_step` = ? where `run_id` = ? and `queue` = ? and `claimed_by` = ? and `state` = ? and (EXISTS (SELECT 1 FROM tasks t\n                     WHERE t.task_id = runs.task_id AND t.queue = runs.queue AND t.state IN ('pending','running','sleeping') AND (t.cancel_at_ms IS NULL\n    OR (((t.cancel_at_ms) IS NOT NULL AND t.cancel_at_ms BETWEEN 0 AND 253402300799000)\n      AND t.cancel_at_ms > COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n))))\n         AND ((runs.attempt) IS NOT NULL AND runs.attempt BETWEEN 1 AND 1000020)\n         AND NOT EXISTS (\n    SELECT 1 FROM checkpoints c\n    WHERE c.task_id = runs.task_id\n      AND c.checkpoint_name = ?\n      AND NOT (\n        c.queue = runs.queue\n        AND EXISTS (\n          SELECT 1 FROM (SELECT owner_run.* FROM runs owner_run\n                         WHERE owner_run.run_id = c.owner_run_id) owner\n          WHERE ((c.owner_attempt) IS NOT NULL AND c.owner_attempt BETWEEN 1 AND 1000020)\n   AND owner.run_id = c.owner_run_id\n   AND owner.task_id = c.task_id\n   AND owner.queue = c.queue\n   AND owner.attempt = c.owner_attempt\n        )\n      )\n  )) and ((CASE WHEN ? = 1 THEN ((COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n)) IS NOT NULL\n    AND (COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n)) BETWEEN 0 AND 253402300799000\n    AND (COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n)) <= 253402300799000 - (CAST((?) AS SIGNED))) ELSE TRUE END))",
        "bindArity": 22
      },
      {
        "sql": "insert into `checkpoints` (`task_id`, `checkpoint_name`, `queue`, `state`, `owner_run_id`, `owner_attempt`, `updated_at_ms`) select * from (select `f`.`task_id` as `task_id`, ? as `checkpoint_name`, `f`.`queue` as `queue`, ? as `state`, `f`.`run_id` as `owner_run_id`, `f`.`attempt` as `owner_attempt`, `f`.`fence_at_ms` as `updated_at_ms` from `runs` as `f` where `f`.`run_id` = ? and (((f.attempt) IS NOT NULL AND f.attempt BETWEEN 1 AND 1000020)) and `f`.`fence_stamp` = ?) as `excluded` on duplicate key update `state` = if(`excluded`.`owner_attempt` >= `checkpoints`.`owner_attempt`, `excluded`.`state`, `checkpoints`.`state`), `owner_run_id` = if(`excluded`.`owner_attempt` >= `checkpoints`.`owner_attempt`, `excluded`.`owner_run_id`, `checkpoints`.`owner_run_id`), `updated_at_ms` = if(`excluded`.`owner_attempt` >= `checkpoints`.`owner_attempt`, `excluded`.`updated_at_ms`, `checkpoints`.`updated_at_ms`), `owner_attempt` = if(`excluded`.`owner_attempt` >= `checkpoints`.`owner_attempt`, `excluded`.`owner_attempt`, `checkpoints`.`owner_attempt`)",
        "bindArity": 4
      },
      {
        "sql": "update `tasks` set `state` = ((SELECT f.state FROM runs f\n               WHERE f.run_id = ? AND f.fence_stamp = ?)), `fence_stamp` = ?, `fence_at_ms` = (select min(`f`.`fence_at_ms`) as `source_instant` from `runs` as `f` where (f.run_id = ?) and `f`.`queue` = `tasks`.`queue` and `f`.`fence_stamp` = ?) where `task_id` in (select `f`.`task_id` from `runs` as `f` where (f.run_id = ?) and `f`.`queue` = `tasks`.`queue` and `f`.`fence_stamp` = ?) and (state IN ('pending','running','sleeping'))",
        "bindArity": 7
      },
      {
        "sql": "delete from `waits` where `run_id` in (select `f`.`run_id` from `runs` as `f` where (f.run_id = ?) and `f`.`fence_stamp` = ?)",
        "bindArity": 2
      }
    ]
  },
  "await-event": {
    "registered": [
      {
        "sql": "insert into `waits` (`run_id`, `step_name`, `queue`, `task_id`, `event_name`, `status`, `timeout_at_ms`, `created_at_ms`, `fence_stamp`, `fence_at_ms`) select * from (select ? as `run_id`, ? as `step_name`, ? as `queue`, ? as `task_id`, ? as `event_name`, ? as `status`, (CASE WHEN CAST(? AS SIGNED) IS NOT NULL THEN COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n) + ? ELSE NULL END) as `timeout_at_ms`, COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n) as `created_at_ms`, ? as `fence_stamp`, COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n) as `fence_at_ms` where not exists (select `events`.`queue` from `events` where `events`.`queue` = ? and `events`.`event_name` = ?) and exists (select `r`.`run_id` from `runs` as `r` inner join `tasks` as `t` on (t.task_id = r.task_id AND t.queue = r.queue) where `r`.`run_id` = ? and `r`.`queue` = ? and `r`.`task_id` = ? and `r`.`claimed_by` = ? and `r`.`state` = ? and (t.state IN ('pending','running','sleeping') AND (t.cancel_at_ms IS NULL\n    OR (((t.cancel_at_ms) IS NOT NULL AND t.cancel_at_ms BETWEEN 0 AND 253402300799000)\n      AND t.cancel_at_ms > COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n))))) and (CAST(? AS SIGNED) IS NULL OR ((COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n)) IS NOT NULL\n    AND (COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n)) BETWEEN 0 AND 253402300799000\n    AND (COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n)) <= 253402300799000 - (CAST((?) AS SIGNED))))) as `excluded` on duplicate key update `run_id` = `waits`.`run_id`",
        "bindArity": 18
      },
      {
        "sql": "update `runs` set `state` = ('sleeping'), `available_at_ms` = ((SELECT f.timeout_at_ms FROM waits f\n                           WHERE f.run_id = ? AND f.step_name = ? AND f.fence_stamp = ?)), `wake_event` = (?), `event_payload` = (NULL), `wake_step` = (?), `claimed_by` = (NULL), `claim_expires_at_ms` = (NULL), `heartbeat_at_ms` = (NULL), `fence_stamp` = ?, `fence_at_ms` = (select min(`f`.`fence_at_ms`) as `source_instant` from `waits` as `f` where (f.run_id = ? AND f.step_name = ? AND f.status = 'waiting') and `f`.`queue` = `runs`.`queue` and `f`.`fence_stamp` = ?) where `run_id` in (select `f`.`run_id` from `waits` as `f` where (f.run_id = ? AND f.step_name = ? AND f.status = 'waiting') and `f`.`queue` = `runs`.`queue` and `f`.`fence_stamp` = ?) and (queue = ? AND task_id = ? AND claimed_by = ? AND state = 'running'\n            AND EXISTS (SELECT 1 FROM tasks t\n                        WHERE t.task_id = runs.task_id AND t.queue = runs.queue AND t.state IN ('pending','running','sleeping')))",
        "bindArity": 15
      },
      {
        "sql": "update `tasks` set `state` = ('sleeping'), `fence_stamp` = ?, `fence_at_ms` = (select min(`f`.`fence_at_ms`) as `source_instant` from `runs` as `f` where (f.run_id = ? AND f.state = 'sleeping') and `f`.`queue` = `tasks`.`queue` and `f`.`fence_stamp` = ?) where `task_id` in (select `f`.`task_id` from `runs` as `f` where (f.run_id = ? AND f.state = 'sleeping') and `f`.`queue` = `tasks`.`queue` and `f`.`fence_stamp` = ?) and (state IN ('pending','running','sleeping'))",
        "bindArity": 5
      },
      {
        "sql": "select `payload`, (CASE WHEN payload IS NULL THEN 'null' ELSE 'text' END) as `payload_type` from `events` where `queue` = ? and `event_name` = ? and exists (select `r`.`run_id` from `runs` as `r` inner join `tasks` as `t` on (t.task_id = r.task_id AND t.queue = r.queue) where `r`.`run_id` = ? and `r`.`queue` = ? and `r`.`task_id` = ? and `r`.`claimed_by` = ? and `r`.`state` = ? and (t.state IN ('pending','running','sleeping')))",
        "bindArity": 7
      }
    ],
    "registered-child": [
      {
        "sql": "insert into `waits` (`run_id`, `step_name`, `queue`, `task_id`, `event_name`, `status`, `timeout_at_ms`, `created_at_ms`, `fence_stamp`, `fence_at_ms`) select * from (select ? as `run_id`, ? as `step_name`, ? as `queue`, ? as `task_id`, ? as `event_name`, ? as `status`, (CASE WHEN CAST(? AS SIGNED) IS NOT NULL THEN COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n) + ? ELSE NULL END) as `timeout_at_ms`, COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n) as `created_at_ms`, ? as `fence_stamp`, COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n) as `fence_at_ms` where not exists (select `events`.`queue` from `events` where `events`.`queue` = ? and `events`.`event_name` = ?) and exists (select `r`.`run_id` from `runs` as `r` inner join `tasks` as `t` on (t.task_id = r.task_id AND t.queue = r.queue) where `r`.`run_id` = ? and `r`.`queue` = ? and `r`.`task_id` = ? and `r`.`claimed_by` = ? and `r`.`state` = ? and (t.state IN ('pending','running','sleeping') AND (t.cancel_at_ms IS NULL\n    OR (((t.cancel_at_ms) IS NOT NULL AND t.cancel_at_ms BETWEEN 0 AND 253402300799000)\n      AND t.cancel_at_ms > COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n))))) and (CAST(? AS SIGNED) IS NULL OR ((COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n)) IS NOT NULL\n    AND (COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n)) BETWEEN 0 AND 253402300799000\n    AND (COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n)) <= 253402300799000 - (CAST((?) AS SIGNED)))) and exists (select `c`.`task_id` from `tasks` as `c` where `c`.`task_id` = ? and `c`.`queue` = ? and `c`.`state` in (?, ?, ?))) as `excluded` on duplicate key update `run_id` = `waits`.`run_id`",
        "bindArity": 23
      },
      {
        "sql": "update `runs` set `state` = ('sleeping'), `available_at_ms` = ((SELECT f.timeout_at_ms FROM waits f\n                           WHERE f.run_id = ? AND f.step_name = ? AND f.fence_stamp = ?)), `wake_event` = (?), `event_payload` = (NULL), `wake_step` = (?), `claimed_by` = (NULL), `claim_expires_at_ms` = (NULL), `heartbeat_at_ms` = (NULL), `fence_stamp` = ?, `fence_at_ms` = (select min(`f`.`fence_at_ms`) as `source_instant` from `waits` as `f` where (f.run_id = ? AND f.step_name = ? AND f.status = 'waiting') and `f`.`queue` = `runs`.`queue` and `f`.`fence_stamp` = ?) where `run_id` in (select `f`.`run_id` from `waits` as `f` where (f.run_id = ? AND f.step_name = ? AND f.status = 'waiting') and `f`.`queue` = `runs`.`queue` and `f`.`fence_stamp` = ?) and (queue = ? AND task_id = ? AND claimed_by = ? AND state = 'running'\n            AND EXISTS (SELECT 1 FROM tasks t\n                        WHERE t.task_id = runs.task_id AND t.queue = runs.queue AND t.state IN ('pending','running','sleeping')))",
        "bindArity": 15
      },
      {
        "sql": "update `tasks` set `state` = ('sleeping'), `fence_stamp` = ?, `fence_at_ms` = (select min(`f`.`fence_at_ms`) as `source_instant` from `runs` as `f` where (f.run_id = ? AND f.state = 'sleeping') and `f`.`queue` = `tasks`.`queue` and `f`.`fence_stamp` = ?) where `task_id` in (select `f`.`task_id` from `runs` as `f` where (f.run_id = ? AND f.state = 'sleeping') and `f`.`queue` = `tasks`.`queue` and `f`.`fence_stamp` = ?) and (state IN ('pending','running','sleeping'))",
        "bindArity": 5
      },
      {
        "sql": "select `payload`, (CASE WHEN payload IS NULL THEN 'null' ELSE 'text' END) as `payload_type` from `events` where `queue` = ? and `event_name` = ? and exists (select `r`.`run_id` from `runs` as `r` inner join `tasks` as `t` on (t.task_id = r.task_id AND t.queue = r.queue) where `r`.`run_id` = ? and `r`.`queue` = ? and `r`.`task_id` = ? and `r`.`claimed_by` = ? and `r`.`state` = ? and (t.state IN ('pending','running','sleeping')))",
        "bindArity": 7
      }
    ]
  },
  "record-task-done": {
    "recorded": [
      {
        "sql": "insert into `events` (`queue`, `event_name`, `payload`, `emitted_at_ms`, `fence_stamp`, `fence_at_ms`) select `c`.`queue` as `queue`, ? as `event_name`, ? as `payload`, COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n) as `emitted_at_ms`, ? as `fence_stamp`, COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n) as `fence_at_ms` from `tasks` as `c` where `c`.`task_id` = ? and `c`.`fence_stamp` <=> ? and not exists (select `e`.`queue` from `events` as `e` where `e`.`queue` = `c`.`queue` and `e`.`event_name` = ?) and exists (select `r`.`run_id` from `runs` as `r` inner join `tasks` as `t` on (t.task_id = r.task_id AND t.queue = r.queue) where `r`.`run_id` = ? and `r`.`queue` = ? and `r`.`task_id` = ? and `r`.`claimed_by` = ? and `r`.`state` = ? and (t.state IN ('pending','running','sleeping')))",
        "bindArity": 11
      },
      {
        "sql": "select `payload`, (CASE WHEN payload IS NULL THEN 'null' ELSE 'text' END) as `payload_type` from `events` where `queue` = ? and `event_name` = ? and exists (select `r`.`run_id` from `runs` as `r` inner join `tasks` as `t` on (t.task_id = r.task_id AND t.queue = r.queue) where `r`.`run_id` = ? and `r`.`queue` = ? and `r`.`task_id` = ? and `r`.`claimed_by` = ? and `r`.`state` = ? and (t.state IN ('pending','running','sleeping')))",
        "bindArity": 7
      }
    ]
  },
  "emit-event": {
    "emitted": [
      {
        "sql": "insert into `events` (`queue`, `event_name`, `payload`, `emitted_at_ms`, `fence_stamp`, `fence_at_ms`) values (?, ?, ?, COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n), ?, COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n)) as `excluded` on duplicate key update `fence_at_ms` = if(not (`events`.`fence_stamp` <=> ?) and (events.payload IS NOT NULL\n         AND ((events.emitted_at_ms) IS NOT NULL AND events.emitted_at_ms BETWEEN 0 AND 253402300799000)), `events`.`emitted_at_ms`, `events`.`fence_at_ms`), `fence_stamp` = if(not (`events`.`fence_stamp` <=> ?) and (events.payload IS NOT NULL\n         AND ((events.emitted_at_ms) IS NOT NULL AND events.emitted_at_ms BETWEEN 0 AND 253402300799000)), ?, `events`.`fence_stamp`)",
        "bindArity": 7
      },
      {
        "sql": "update `runs` set `state` = ?, `wake_step` = coalesce(`wake_step`, ((SELECT MIN(w.step_name)\n           FROM waits w\n           WHERE w.run_id = runs.run_id\n      AND w.queue = runs.queue\n      AND w.task_id = runs.task_id\n      AND w.event_name = runs.wake_event\n      AND w.status = 'waiting'\n      AND w.timeout_at_ms <=> runs.available_at_ms\n      AND (runs.available_at_ms IS NULL\n        OR ((runs.available_at_ms) IS NOT NULL AND runs.available_at_ms BETWEEN 0 AND 253402300799000))\n      AND (w.timeout_at_ms IS NULL\n        OR ((w.timeout_at_ms) IS NOT NULL AND w.timeout_at_ms BETWEEN 0 AND 253402300799000))\n           GROUP BY w.run_id\n            HAVING COUNT(*) = 1))), `available_at_ms` = (select `f`.`fence_at_ms` from `events` as `f` where `f`.`queue` = `runs`.`queue` and `f`.`event_name` = ? and `f`.`fence_stamp` = ?), `wake_event` = ?, `event_payload` = (select `f`.`payload` from `events` as `f` where `f`.`queue` = `runs`.`queue` and `f`.`event_name` = ? and `f`.`fence_stamp` = ?), `fence_stamp` = ?, `fence_at_ms` = (select `f`.`fence_at_ms` from `events` as `f` where `f`.`queue` = `runs`.`queue` and `f`.`event_name` = ? and `f`.`fence_stamp` = ?) where `state` = ? and (wake_event = ?) and `run_id` in (SELECT w.run_id FROM waits w\n                        WHERE w.queue = ? AND w.event_name = ? AND w.status = 'waiting') and ((runs.wake_step IS NOT NULL AND EXISTS (SELECT 1 FROM waits w\n                      WHERE w.run_id = runs.run_id\n      AND w.queue = runs.queue\n      AND w.task_id = runs.task_id\n      AND w.event_name = runs.wake_event\n      AND w.status = 'waiting'\n      AND w.timeout_at_ms <=> runs.available_at_ms\n      AND (runs.available_at_ms IS NULL\n        OR ((runs.available_at_ms) IS NOT NULL AND runs.available_at_ms BETWEEN 0 AND 253402300799000))\n      AND (w.timeout_at_ms IS NULL\n        OR ((w.timeout_at_ms) IS NOT NULL AND w.timeout_at_ms BETWEEN 0 AND 253402300799000))\n                        AND w.step_name = runs.wake_step))\n              OR (runs.wake_step IS NULL AND (SELECT MIN(w.step_name)\n           FROM waits w\n           WHERE w.run_id = runs.run_id\n      AND w.queue = runs.queue\n      AND w.task_id = runs.task_id\n      AND w.event_name = runs.wake_event\n      AND w.status = 'waiting'\n      AND w.timeout_at_ms <=> runs.available_at_ms\n      AND (runs.available_at_ms IS NULL\n        OR ((runs.available_at_ms) IS NOT NULL AND runs.available_at_ms BETWEEN 0 AND 253402300799000))\n      AND (w.timeout_at_ms IS NULL\n        OR ((w.timeout_at_ms) IS NOT NULL AND w.timeout_at_ms BETWEEN 0 AND 253402300799000))\n           GROUP BY w.run_id\n            HAVING COUNT(*) = 1) IS NOT NULL)) and exists (select `f`.`queue` from `events` as `f` where `f`.`queue` = `runs`.`queue` and `f`.`event_name` = ? and `f`.`fence_stamp` = ?) and (EXISTS (SELECT 1 FROM tasks t\n                     WHERE t.task_id = runs.task_id AND t.queue = runs.queue AND t.state IN ('pending','running','sleeping')))",
        "bindArity": 15
      },
      {
        "sql": "update `tasks` set `state` = ('pending'), `fence_stamp` = ?, `fence_at_ms` = (select min(`f`.`fence_at_ms`) as `source_instant` from `runs` as `f` where (f.wake_event = ? AND f.state = 'pending') and `f`.`queue` = ? and `f`.`fence_stamp` = ?) where `task_id` in (select `f`.`task_id` from `runs` as `f` where (f.wake_event = ? AND f.state = 'pending') and `f`.`queue` = ? and `f`.`fence_stamp` = ?) and `tasks`.`queue` = ? and (state IN ('pending','running','sleeping'))",
        "bindArity": 8
      },
      {
        "sql": "delete from `waits` where `run_id` in (select `f`.`run_id` from `runs` as `f` where (f.queue = ? AND f.wake_event = ? AND f.state = 'pending') and `f`.`fence_stamp` = ?) and (event_name = ? AND status = 'waiting')",
        "bindArity": 4
      },
      {
        "sql": "update `runs` set `run_id` = `run_id`, `fence_stamp` = ?, `fence_at_ms` = (select min(`source_fence_at_ms`) as `source_instant` from (select distinct `f`.`fence_at_ms` as `source_fence_at_ms` from `runs` as `f` where (f.queue = ? AND f.wake_event = ? AND f.state = 'pending') and `f`.`fence_stamp` = ?) as `fenced_source_instant`) where `run_id` in (select `source_key` from (select distinct `f`.`run_id` as `source_key` from `runs` as `f` where (f.queue = ? AND f.wake_event = ? AND f.state = 'pending') and `f`.`fence_stamp` = ?) as `fenced_source`)",
        "bindArity": 7
      },
      {
        "sql": "select (CASE WHEN payload IS NULL THEN 'null' ELSE 'text' END) as `payload_type` from `events` where `queue` = ? and `event_name` = ?",
        "bindArity": 2
      }
    ]
  },
  "set-checkpoint": {
    "written": [
      {
        "sql": "update `runs` set `claim_expires_at_ms` = (COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n) + ?), `heartbeat_at_ms` = COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n), `fence_stamp` = ?, `fence_at_ms` = COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n) where `run_id` = ? and `queue` = ? and `claimed_by` = ? and `state` = ? and `task_id` = ? and (((runs.attempt) IS NOT NULL AND runs.attempt BETWEEN 1 AND 1000020)\n         AND EXISTS (SELECT 1 FROM tasks t\n                     WHERE t.task_id = runs.task_id AND t.queue = runs.queue AND t.state IN ('pending','running','sleeping'))\n         AND NOT EXISTS (\n    SELECT 1 FROM checkpoints c\n    WHERE c.task_id = runs.task_id\n      AND c.checkpoint_name = ?\n      AND NOT (\n        c.queue = runs.queue\n        AND EXISTS (\n          SELECT 1 FROM (SELECT owner_run.* FROM runs owner_run\n                         WHERE owner_run.run_id = c.owner_run_id) owner\n          WHERE ((c.owner_attempt) IS NOT NULL AND c.owner_attempt BETWEEN 1 AND 1000020)\n   AND owner.run_id = c.owner_run_id\n   AND owner.task_id = c.task_id\n   AND owner.queue = c.queue\n   AND owner.attempt = c.owner_attempt\n        )\n      )\n  )) and (((COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n)) IS NOT NULL\n    AND (COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n)) BETWEEN 0 AND 253402300799000\n    AND (COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n)) <= 253402300799000 - (CAST((?) AS SIGNED))))",
        "bindArity": 9
      },
      {
        "sql": "insert into `checkpoints` (`task_id`, `checkpoint_name`, `queue`, `state`, `owner_run_id`, `owner_attempt`, `updated_at_ms`) select * from (select `f`.`task_id` as `task_id`, ? as `checkpoint_name`, `f`.`queue` as `queue`, ? as `state`, `f`.`run_id` as `owner_run_id`, `f`.`attempt` as `owner_attempt`, `f`.`fence_at_ms` as `updated_at_ms` from `runs` as `f` where `f`.`run_id` = ? and (((f.attempt) IS NOT NULL AND f.attempt BETWEEN 1 AND 1000020)) and `f`.`fence_stamp` = ?) as `excluded` on duplicate key update `state` = if(`excluded`.`owner_attempt` >= `checkpoints`.`owner_attempt`, `excluded`.`state`, `checkpoints`.`state`), `owner_run_id` = if(`excluded`.`owner_attempt` >= `checkpoints`.`owner_attempt`, `excluded`.`owner_run_id`, `checkpoints`.`owner_run_id`), `updated_at_ms` = if(`excluded`.`owner_attempt` >= `checkpoints`.`owner_attempt`, `excluded`.`updated_at_ms`, `checkpoints`.`updated_at_ms`), `owner_attempt` = if(`excluded`.`owner_attempt` >= `checkpoints`.`owner_attempt`, `excluded`.`owner_attempt`, `checkpoints`.`owner_attempt`)",
        "bindArity": 4
      }
    ]
  },
  "fail": {
    "retrying": [
      {
        "sql": "update `runs` set `state` = ?, `failed_at_ms` = COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n), `failure_reason` = ?, `claimed_by` = ?, `claim_expires_at_ms` = ?, `fence_stamp` = ?, `fence_at_ms` = COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n) where `run_id` = ? and `queue` = ? and `claimed_by` = ? and `state` = ? and (EXISTS (\n           SELECT 1 FROM tasks t\n           WHERE t.task_id = runs.task_id AND t.queue = runs.queue\n             AND (t.state NOT IN ('pending','running','sleeping')\n               OR (t.state IN ('pending','running','sleeping')\n                 AND NOT EXISTS (SELECT 1 FROM (SELECT sibling_run.* FROM runs sibling_run\n    WHERE sibling_run.task_id = runs.task_id) sibling\n               WHERE sibling.state IN ('pending','running','sleeping')\n                 AND sibling.run_id <> runs.run_id)\n                 AND (((runs.attempt) IS NOT NULL AND runs.attempt BETWEEN 1 AND 1000020)\n    AND ((t.attempts) IS NOT NULL AND t.attempts BETWEEN 0 AND 1000000)\n    AND ((t.max_attempts) IS NOT NULL AND t.max_attempts BETWEEN 1 AND 1000000)\n    AND ((t.infra_retries) IS NOT NULL AND t.infra_retries BETWEEN 0 AND 20)\n    AND t.attempts < t.max_attempts\n    AND runs.attempt = t.attempts + t.infra_retries + 1)\n                 AND NOT EXISTS (\n    SELECT 1 FROM (SELECT higher_run.* FROM runs higher_run\n    WHERE higher_run.task_id = runs.task_id) higher\n    WHERE (NOT ((higher.attempt) IS NOT NULL AND higher.attempt BETWEEN 1 AND 1000020)\n        OR higher.attempt > runs.attempt)\n  )\n                 AND ((runs.attempt - t.infra_retries) >= t.max_attempts\n          OR ((COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n)) IS NOT NULL\n    AND (COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n)) BETWEEN 0 AND 253402300799000\n    AND (COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n)) <= 253402300799000 - (CAST((?) AS SIGNED))))))\n         ))",
        "bindArity": 10
      },
      {
        "sql": "insert into `runs` (`run_id`, `queue`, `task_id`, `attempt`, `state`, `available_at_ms`, `created_at_ms`, `wake_event`, `event_payload`, `wake_step`, `run_db`, `fence_stamp`, `fence_at_ms`) select ? as `run_id`, `f`.`queue` as `queue`, `f`.`task_id` as `task_id`, `f`.`attempt` + ? as `attempt`, ? as `state`, `f`.`fence_at_ms` + ? as `available_at_ms`, `f`.`fence_at_ms` as `created_at_ms`, `f`.`wake_event` as `wake_event`, `f`.`event_payload` as `event_payload`, `f`.`wake_step` as `wake_step`, `f`.`run_db` as `run_db`, ? as `fence_stamp`, `f`.`fence_at_ms` as `fence_at_ms` from `runs` as `f` inner join `tasks` as `t` on (t.task_id = f.task_id AND t.queue = f.queue) where `f`.`run_id` = ? and `f`.`fence_stamp` = ? and (t.state IN ('pending','running','sleeping') AND (f.attempt - t.infra_retries) < t.max_attempts\n           AND ((f.attempt) IS NOT NULL AND f.attempt BETWEEN 1 AND 1000019)) and (NOT EXISTS (SELECT 1 FROM runs s\n           WHERE s.run_id = ? AND s.task_id = f.task_id\n             AND s.attempt = f.attempt + 1))",
        "bindArity": 8
      },
      {
        "sql": "update `tasks` set `attempts` = ((SELECT f.attempt - tasks.infra_retries FROM runs f\n    WHERE f.run_id = ? AND f.fence_stamp = ?)), `state` = ((SELECT f.state FROM runs f\n                   WHERE f.run_id = ? AND f.fence_stamp = ?)), `last_attempt_run` = (?), `fence_stamp` = ?, `fence_at_ms` = (select min(`f`.`fence_at_ms`) as `source_instant` from `runs` as `f` where (f.run_id = ?) and `f`.`queue` = `tasks`.`queue` and `f`.`fence_stamp` = ?) where `task_id` in (select `f`.`task_id` from `runs` as `f` where (f.run_id = ?) and `f`.`queue` = `tasks`.`queue` and `f`.`fence_stamp` = ?) and (state IN ('pending','running','sleeping'))",
        "bindArity": 10
      },
      {
        "sql": "update `tasks` set `attempts` = ((SELECT f.attempt - tasks.infra_retries FROM runs f\n    WHERE f.run_id = ? AND f.fence_stamp = ?)), `state` = ('failed'), `failure_reason` = (?), `fence_stamp` = ?, `fence_at_ms` = (select min(`f`.`fence_at_ms`) as `source_instant` from `runs` as `f` where (f.run_id = ?) and `f`.`queue` = `tasks`.`queue` and `f`.`fence_stamp` = ?) where `task_id` in (select `f`.`task_id` from `runs` as `f` where (f.run_id = ?) and `f`.`queue` = `tasks`.`queue` and `f`.`fence_stamp` = ?) and (state IN ('pending','running','sleeping')\n            AND NOT EXISTS (SELECT 1 FROM runs s\n           WHERE s.run_id = ? AND s.task_id = tasks.task_id\n             AND s.attempt = (SELECT p.attempt + 1 FROM runs p WHERE p.run_id = ?)))",
        "bindArity": 10
      },
      {
        "sql": "delete from `waits` where `run_id` in (select `f`.`run_id` from `runs` as `f` where (f.run_id = ?) and `f`.`fence_stamp` = ?)",
        "bindArity": 2
      },
      {
        "sql": "insert into `events` (`queue`, `event_name`, `payload`, `emitted_at_ms`, `fence_stamp`, `fence_at_ms`) select `f`.`queue` as `queue`, ? as `event_name`, ? as `payload`, `f`.`fence_at_ms` as `emitted_at_ms`, ? as `fence_stamp`, `f`.`fence_at_ms` as `fence_at_ms` from `tasks` as `f` where `f`.`task_id` = ? and `f`.`queue` = ? and `f`.`fence_stamp` = ? and not exists (select `e`.`queue` from `events` as `e` where `e`.`queue` = `f`.`queue` and `e`.`event_name` = ?)",
        "bindArity": 7
      },
      {
        "sql": "update `runs` set `state` = ?, `wake_step` = coalesce(`wake_step`, ((SELECT MIN(w.step_name)\n           FROM waits w\n           WHERE w.run_id = runs.run_id\n      AND w.queue = runs.queue\n      AND w.task_id = runs.task_id\n      AND w.event_name = runs.wake_event\n      AND w.status = 'waiting'\n      AND w.timeout_at_ms <=> runs.available_at_ms\n      AND (runs.available_at_ms IS NULL\n        OR ((runs.available_at_ms) IS NOT NULL AND runs.available_at_ms BETWEEN 0 AND 253402300799000))\n      AND (w.timeout_at_ms IS NULL\n        OR ((w.timeout_at_ms) IS NOT NULL AND w.timeout_at_ms BETWEEN 0 AND 253402300799000))\n           GROUP BY w.run_id\n            HAVING COUNT(*) = 1))), `available_at_ms` = (select `f`.`fence_at_ms` from `events` as `f` where `f`.`queue` = `runs`.`queue` and `f`.`event_name` = ? and `f`.`fence_stamp` = ?), `wake_event` = ?, `event_payload` = (select `f`.`payload` from `events` as `f` where `f`.`queue` = `runs`.`queue` and `f`.`event_name` = ? and `f`.`fence_stamp` = ?), `fence_stamp` = ?, `fence_at_ms` = (select `f`.`fence_at_ms` from `events` as `f` where `f`.`queue` = `runs`.`queue` and `f`.`event_name` = ? and `f`.`fence_stamp` = ?) where `state` = ? and (wake_event = ?) and `run_id` in (SELECT w.run_id FROM waits w\n                        WHERE w.queue = ? AND w.event_name = ? AND w.status = 'waiting') and ((runs.wake_step IS NOT NULL AND EXISTS (SELECT 1 FROM waits w\n                      WHERE w.run_id = runs.run_id\n      AND w.queue = runs.queue\n      AND w.task_id = runs.task_id\n      AND w.event_name = runs.wake_event\n      AND w.status = 'waiting'\n      AND w.timeout_at_ms <=> runs.available_at_ms\n      AND (runs.available_at_ms IS NULL\n        OR ((runs.available_at_ms) IS NOT NULL AND runs.available_at_ms BETWEEN 0 AND 253402300799000))\n      AND (w.timeout_at_ms IS NULL\n        OR ((w.timeout_at_ms) IS NOT NULL AND w.timeout_at_ms BETWEEN 0 AND 253402300799000))\n                        AND w.step_name = runs.wake_step))\n              OR (runs.wake_step IS NULL AND (SELECT MIN(w.step_name)\n           FROM waits w\n           WHERE w.run_id = runs.run_id\n      AND w.queue = runs.queue\n      AND w.task_id = runs.task_id\n      AND w.event_name = runs.wake_event\n      AND w.status = 'waiting'\n      AND w.timeout_at_ms <=> runs.available_at_ms\n      AND (runs.available_at_ms IS NULL\n        OR ((runs.available_at_ms) IS NOT NULL AND runs.available_at_ms BETWEEN 0 AND 253402300799000))\n      AND (w.timeout_at_ms IS NULL\n        OR ((w.timeout_at_ms) IS NOT NULL AND w.timeout_at_ms BETWEEN 0 AND 253402300799000))\n           GROUP BY w.run_id\n            HAVING COUNT(*) = 1) IS NOT NULL)) and exists (select `f`.`queue` from `events` as `f` where `f`.`queue` = `runs`.`queue` and `f`.`event_name` = ? and `f`.`fence_stamp` = ?) and (EXISTS (SELECT 1 FROM tasks t\n                     WHERE t.task_id = runs.task_id AND t.queue = runs.queue AND t.state IN ('pending','running','sleeping')))",
        "bindArity": 15
      },
      {
        "sql": "update `tasks` set `state` = ('pending'), `fence_stamp` = ?, `fence_at_ms` = (select min(`f`.`fence_at_ms`) as `source_instant` from `runs` as `f` where (f.wake_event = ? AND f.state = 'pending') and `f`.`queue` = ? and `f`.`fence_stamp` = ?) where `task_id` in (select `f`.`task_id` from `runs` as `f` where (f.wake_event = ? AND f.state = 'pending') and `f`.`queue` = ? and `f`.`fence_stamp` = ?) and `tasks`.`queue` = ? and (state IN ('pending','running','sleeping'))",
        "bindArity": 8
      },
      {
        "sql": "delete from `waits` where `run_id` in (select `f`.`run_id` from `runs` as `f` where (f.queue = ? AND f.wake_event = ? AND f.state = 'pending') and `f`.`fence_stamp` = ?) and (event_name = ? AND status = 'waiting')",
        "bindArity": 4
      },
      {
        "sql": "update `runs` set `run_id` = `run_id`, `fence_stamp` = ?, `fence_at_ms` = (select min(`source_fence_at_ms`) as `source_instant` from (select distinct `f`.`fence_at_ms` as `source_fence_at_ms` from `runs` as `f` where (f.queue = ? AND f.wake_event = ? AND f.state = 'pending') and `f`.`fence_stamp` = ?) as `fenced_source_instant`) where `run_id` in (select `source_key` from (select distinct `f`.`run_id` as `source_key` from `runs` as `f` where (f.queue = ? AND f.wake_event = ? AND f.state = 'pending') and `f`.`fence_stamp` = ?) as `fenced_source`)",
        "bindArity": 7
      }
    ],
    "final": [
      {
        "sql": "update `runs` set `state` = ?, `failed_at_ms` = COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n), `failure_reason` = ?, `claimed_by` = ?, `claim_expires_at_ms` = ?, `fence_stamp` = ?, `fence_at_ms` = COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n) where `run_id` = ? and `queue` = ? and `claimed_by` = ? and `state` = ? and (EXISTS (\n           SELECT 1 FROM tasks t\n           WHERE t.task_id = runs.task_id AND t.queue = runs.queue\n             AND (t.state NOT IN ('pending','running','sleeping')\n               OR (t.state IN ('pending','running','sleeping')\n                 AND NOT EXISTS (SELECT 1 FROM (SELECT sibling_run.* FROM runs sibling_run\n    WHERE sibling_run.task_id = runs.task_id) sibling\n               WHERE sibling.state IN ('pending','running','sleeping')\n                 AND sibling.run_id <> runs.run_id)\n                 AND (((runs.attempt) IS NOT NULL AND runs.attempt BETWEEN 1 AND 1000020)\n    AND ((t.attempts) IS NOT NULL AND t.attempts BETWEEN 0 AND 1000000)\n    AND ((t.max_attempts) IS NOT NULL AND t.max_attempts BETWEEN 1 AND 1000000)\n    AND ((t.infra_retries) IS NOT NULL AND t.infra_retries BETWEEN 0 AND 20)\n    AND t.attempts < t.max_attempts\n    AND runs.attempt = t.attempts + t.infra_retries + 1)\n                 AND NOT EXISTS (\n    SELECT 1 FROM (SELECT higher_run.* FROM runs higher_run\n    WHERE higher_run.task_id = runs.task_id) higher\n    WHERE (NOT ((higher.attempt) IS NOT NULL AND higher.attempt BETWEEN 1 AND 1000020)\n        OR higher.attempt > runs.attempt)\n  )\n                 ))\n         ))",
        "bindArity": 9
      },
      {
        "sql": "update `tasks` set `attempts` = ((SELECT f.attempt - tasks.infra_retries FROM runs f\n    WHERE f.run_id = ? AND f.fence_stamp = ?)), `state` = ('failed'), `failure_reason` = (?), `fence_stamp` = ?, `fence_at_ms` = (select min(`f`.`fence_at_ms`) as `source_instant` from `runs` as `f` where (f.run_id = ?) and `f`.`queue` = `tasks`.`queue` and `f`.`fence_stamp` = ?) where `task_id` in (select `f`.`task_id` from `runs` as `f` where (f.run_id = ?) and `f`.`queue` = `tasks`.`queue` and `f`.`fence_stamp` = ?) and (state IN ('pending','running','sleeping'))",
        "bindArity": 8
      },
      {
        "sql": "delete from `waits` where `run_id` in (select `f`.`run_id` from `runs` as `f` where (f.run_id = ?) and `f`.`fence_stamp` = ?)",
        "bindArity": 2
      },
      {
        "sql": "insert into `events` (`queue`, `event_name`, `payload`, `emitted_at_ms`, `fence_stamp`, `fence_at_ms`) select `f`.`queue` as `queue`, ? as `event_name`, ? as `payload`, `f`.`fence_at_ms` as `emitted_at_ms`, ? as `fence_stamp`, `f`.`fence_at_ms` as `fence_at_ms` from `tasks` as `f` where `f`.`task_id` = ? and `f`.`queue` = ? and `f`.`fence_stamp` = ? and not exists (select `e`.`queue` from `events` as `e` where `e`.`queue` = `f`.`queue` and `e`.`event_name` = ?)",
        "bindArity": 7
      },
      {
        "sql": "update `runs` set `state` = ?, `wake_step` = coalesce(`wake_step`, ((SELECT MIN(w.step_name)\n           FROM waits w\n           WHERE w.run_id = runs.run_id\n      AND w.queue = runs.queue\n      AND w.task_id = runs.task_id\n      AND w.event_name = runs.wake_event\n      AND w.status = 'waiting'\n      AND w.timeout_at_ms <=> runs.available_at_ms\n      AND (runs.available_at_ms IS NULL\n        OR ((runs.available_at_ms) IS NOT NULL AND runs.available_at_ms BETWEEN 0 AND 253402300799000))\n      AND (w.timeout_at_ms IS NULL\n        OR ((w.timeout_at_ms) IS NOT NULL AND w.timeout_at_ms BETWEEN 0 AND 253402300799000))\n           GROUP BY w.run_id\n            HAVING COUNT(*) = 1))), `available_at_ms` = (select `f`.`fence_at_ms` from `events` as `f` where `f`.`queue` = `runs`.`queue` and `f`.`event_name` = ? and `f`.`fence_stamp` = ?), `wake_event` = ?, `event_payload` = (select `f`.`payload` from `events` as `f` where `f`.`queue` = `runs`.`queue` and `f`.`event_name` = ? and `f`.`fence_stamp` = ?), `fence_stamp` = ?, `fence_at_ms` = (select `f`.`fence_at_ms` from `events` as `f` where `f`.`queue` = `runs`.`queue` and `f`.`event_name` = ? and `f`.`fence_stamp` = ?) where `state` = ? and (wake_event = ?) and `run_id` in (SELECT w.run_id FROM waits w\n                        WHERE w.queue = ? AND w.event_name = ? AND w.status = 'waiting') and ((runs.wake_step IS NOT NULL AND EXISTS (SELECT 1 FROM waits w\n                      WHERE w.run_id = runs.run_id\n      AND w.queue = runs.queue\n      AND w.task_id = runs.task_id\n      AND w.event_name = runs.wake_event\n      AND w.status = 'waiting'\n      AND w.timeout_at_ms <=> runs.available_at_ms\n      AND (runs.available_at_ms IS NULL\n        OR ((runs.available_at_ms) IS NOT NULL AND runs.available_at_ms BETWEEN 0 AND 253402300799000))\n      AND (w.timeout_at_ms IS NULL\n        OR ((w.timeout_at_ms) IS NOT NULL AND w.timeout_at_ms BETWEEN 0 AND 253402300799000))\n                        AND w.step_name = runs.wake_step))\n              OR (runs.wake_step IS NULL AND (SELECT MIN(w.step_name)\n           FROM waits w\n           WHERE w.run_id = runs.run_id\n      AND w.queue = runs.queue\n      AND w.task_id = runs.task_id\n      AND w.event_name = runs.wake_event\n      AND w.status = 'waiting'\n      AND w.timeout_at_ms <=> runs.available_at_ms\n      AND (runs.available_at_ms IS NULL\n        OR ((runs.available_at_ms) IS NOT NULL AND runs.available_at_ms BETWEEN 0 AND 253402300799000))\n      AND (w.timeout_at_ms IS NULL\n        OR ((w.timeout_at_ms) IS NOT NULL AND w.timeout_at_ms BETWEEN 0 AND 253402300799000))\n           GROUP BY w.run_id\n            HAVING COUNT(*) = 1) IS NOT NULL)) and exists (select `f`.`queue` from `events` as `f` where `f`.`queue` = `runs`.`queue` and `f`.`event_name` = ? and `f`.`fence_stamp` = ?) and (EXISTS (SELECT 1 FROM tasks t\n                     WHERE t.task_id = runs.task_id AND t.queue = runs.queue AND t.state IN ('pending','running','sleeping')))",
        "bindArity": 15
      },
      {
        "sql": "update `tasks` set `state` = ('pending'), `fence_stamp` = ?, `fence_at_ms` = (select min(`f`.`fence_at_ms`) as `source_instant` from `runs` as `f` where (f.wake_event = ? AND f.state = 'pending') and `f`.`queue` = ? and `f`.`fence_stamp` = ?) where `task_id` in (select `f`.`task_id` from `runs` as `f` where (f.wake_event = ? AND f.state = 'pending') and `f`.`queue` = ? and `f`.`fence_stamp` = ?) and `tasks`.`queue` = ? and (state IN ('pending','running','sleeping'))",
        "bindArity": 8
      },
      {
        "sql": "delete from `waits` where `run_id` in (select `f`.`run_id` from `runs` as `f` where (f.queue = ? AND f.wake_event = ? AND f.state = 'pending') and `f`.`fence_stamp` = ?) and (event_name = ? AND status = 'waiting')",
        "bindArity": 4
      },
      {
        "sql": "update `runs` set `run_id` = `run_id`, `fence_stamp` = ?, `fence_at_ms` = (select min(`source_fence_at_ms`) as `source_instant` from (select distinct `f`.`fence_at_ms` as `source_fence_at_ms` from `runs` as `f` where (f.queue = ? AND f.wake_event = ? AND f.state = 'pending') and `f`.`fence_stamp` = ?) as `fenced_source_instant`) where `run_id` in (select `source_key` from (select distinct `f`.`run_id` as `source_key` from `runs` as `f` where (f.queue = ? AND f.wake_event = ? AND f.state = 'pending') and `f`.`fence_stamp` = ?) as `fenced_source`)",
        "bindArity": 7
      }
    ]
  },
  "retry-task": {
    "revived": [
      {
        "sql": "update `tasks` set `state` = ?, `attempts` = (((SELECT MAX(r.attempt) FROM runs r WHERE tasks.task_id = r.task_id AND tasks.queue = r.queue) - infra_retries)), `max_attempts` = `max_attempts` + ?, `failure_reason` = ?, `last_attempt_run` = ?, `fence_stamp` = ?, `fence_at_ms` = COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n) where `task_id` = ? and `queue` = ? and `state` = ? and (NOT EXISTS (\n    SELECT 1 FROM runs ownership_run\n    WHERE ownership_run.task_id = tasks.task_id\n      AND ownership_run.queue <> tasks.queue\n  )\n         AND failure_reason IS NOT NULL AND completed_payload IS NULL\n         AND EXISTS (SELECT 1 FROM runs r WHERE tasks.task_id = r.task_id AND tasks.queue = r.queue)\n         AND NOT EXISTS (SELECT 1 FROM runs r\n                   WHERE tasks.task_id = r.task_id AND tasks.queue = r.queue AND r.state IN ('pending','running','sleeping'))\n         AND ((tasks.attempts) IS NOT NULL AND tasks.attempts BETWEEN 0 AND 1000000)\n         AND ((tasks.infra_retries) IS NOT NULL AND tasks.infra_retries BETWEEN 0 AND 20)\n         AND NOT EXISTS (SELECT 1 FROM runs r\n                         WHERE tasks.task_id = r.task_id AND tasks.queue = r.queue\n                           AND NOT ((r.attempt) IS NOT NULL AND r.attempt BETWEEN 1 AND 1000020))\n         AND ((tasks.max_attempts) IS NOT NULL AND tasks.max_attempts BETWEEN 1 AND 999999)\n         AND ((SELECT MAX(r.attempt) FROM runs r WHERE tasks.task_id = r.task_id AND tasks.queue = r.queue) - infra_retries) - attempts IN (0, 1)\n         AND ((SELECT MAX(r.attempt) FROM runs r WHERE tasks.task_id = r.task_id AND tasks.queue = r.queue) - infra_retries) <= max_attempts)",
        "bindArity": 8
      },
      {
        "sql": "insert into `runs` (`run_id`, `queue`, `task_id`, `attempt`, `state`, `available_at_ms`, `created_at_ms`, `wake_event`, `event_payload`, `wake_step`, `run_db`, `fence_stamp`, `fence_at_ms`) select ? as `run_id`, `f`.`queue` as `queue`, `f`.`task_id` as `task_id`, `p`.`attempt` + ? as `attempt`, ? as `state`, `f`.`fence_at_ms` as `available_at_ms`, `f`.`fence_at_ms` as `created_at_ms`, `p`.`wake_event` as `wake_event`, `p`.`event_payload` as `event_payload`, `p`.`wake_step` as `wake_step`, `p`.`run_db` as `run_db`, ? as `fence_stamp`, `f`.`fence_at_ms` as `fence_at_ms` from `tasks` as `f` inner join `runs` as `p` on (f.task_id = p.task_id AND f.queue = p.queue) where `f`.`task_id` = ? and `f`.`fence_stamp` = ? and (p.attempt = (SELECT MAX(r.attempt) FROM runs r WHERE f.task_id = r.task_id AND f.queue = r.queue)) and (NOT EXISTS (SELECT 1 FROM runs r\n                   WHERE f.task_id = r.task_id AND f.queue = r.queue AND r.state IN ('pending','running','sleeping')))",
        "bindArity": 6
      },
      {
        "sql": "select `attempt` from `runs` where `run_id` = ? and `fence_stamp` = ?",
        "bindArity": 2
      }
    ]
  },
  "cancel-task": {
    "cancelled": [
      {
        "sql": "update `tasks` set `state` = ?, `cancelled_at_ms` = COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n), `cancel_at_ms` = ?, `failure_reason` = ?, `fence_stamp` = ?, `fence_at_ms` = COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n) where `task_id` = ? and `queue` = ? and `state` in (?, ?, ?) and (NOT EXISTS (\n    SELECT 1 FROM runs ownership_run\n    WHERE ownership_run.task_id = tasks.task_id\n      AND ownership_run.queue <> tasks.queue\n  ))",
        "bindArity": 9
      },
      {
        "sql": "update `runs` set `state` = ('cancelled'), `claimed_by` = (NULL), `claim_expires_at_ms` = (NULL), `fence_stamp` = ?, `fence_at_ms` = (select min(`f`.`fence_at_ms`) as `source_instant` from `tasks` as `f` where (f.task_id = ?) and `f`.`queue` = `runs`.`queue` and `f`.`fence_stamp` = ?) where `task_id` in (select `f`.`task_id` from `tasks` as `f` where (f.task_id = ?) and `f`.`queue` = `runs`.`queue` and `f`.`fence_stamp` = ?) and (state IN ('pending','running','sleeping'))",
        "bindArity": 5
      },
      {
        "sql": "delete from `waits` where `run_id` in (select `f`.`run_id` from `runs` as `f` where (f.task_id = ? AND f.state = 'cancelled') and `f`.`fence_stamp` = ?)",
        "bindArity": 2
      },
      {
        "sql": "insert into `events` (`queue`, `event_name`, `payload`, `emitted_at_ms`, `fence_stamp`, `fence_at_ms`) select `f`.`queue` as `queue`, ? as `event_name`, ? as `payload`, `f`.`fence_at_ms` as `emitted_at_ms`, ? as `fence_stamp`, `f`.`fence_at_ms` as `fence_at_ms` from `tasks` as `f` where `f`.`task_id` = ? and `f`.`queue` = ? and `f`.`fence_stamp` = ? and not exists (select `e`.`queue` from `events` as `e` where `e`.`queue` = `f`.`queue` and `e`.`event_name` = ?)",
        "bindArity": 7
      },
      {
        "sql": "update `runs` set `state` = ?, `wake_step` = coalesce(`wake_step`, ((SELECT MIN(w.step_name)\n           FROM waits w\n           WHERE w.run_id = runs.run_id\n      AND w.queue = runs.queue\n      AND w.task_id = runs.task_id\n      AND w.event_name = runs.wake_event\n      AND w.status = 'waiting'\n      AND w.timeout_at_ms <=> runs.available_at_ms\n      AND (runs.available_at_ms IS NULL\n        OR ((runs.available_at_ms) IS NOT NULL AND runs.available_at_ms BETWEEN 0 AND 253402300799000))\n      AND (w.timeout_at_ms IS NULL\n        OR ((w.timeout_at_ms) IS NOT NULL AND w.timeout_at_ms BETWEEN 0 AND 253402300799000))\n           GROUP BY w.run_id\n            HAVING COUNT(*) = 1))), `available_at_ms` = (select `f`.`fence_at_ms` from `events` as `f` where `f`.`queue` = `runs`.`queue` and `f`.`event_name` = ? and `f`.`fence_stamp` = ?), `wake_event` = ?, `event_payload` = (select `f`.`payload` from `events` as `f` where `f`.`queue` = `runs`.`queue` and `f`.`event_name` = ? and `f`.`fence_stamp` = ?), `fence_stamp` = ?, `fence_at_ms` = (select `f`.`fence_at_ms` from `events` as `f` where `f`.`queue` = `runs`.`queue` and `f`.`event_name` = ? and `f`.`fence_stamp` = ?) where `state` = ? and (wake_event = ?) and `run_id` in (SELECT w.run_id FROM waits w\n                        WHERE w.queue = ? AND w.event_name = ? AND w.status = 'waiting') and ((runs.wake_step IS NOT NULL AND EXISTS (SELECT 1 FROM waits w\n                      WHERE w.run_id = runs.run_id\n      AND w.queue = runs.queue\n      AND w.task_id = runs.task_id\n      AND w.event_name = runs.wake_event\n      AND w.status = 'waiting'\n      AND w.timeout_at_ms <=> runs.available_at_ms\n      AND (runs.available_at_ms IS NULL\n        OR ((runs.available_at_ms) IS NOT NULL AND runs.available_at_ms BETWEEN 0 AND 253402300799000))\n      AND (w.timeout_at_ms IS NULL\n        OR ((w.timeout_at_ms) IS NOT NULL AND w.timeout_at_ms BETWEEN 0 AND 253402300799000))\n                        AND w.step_name = runs.wake_step))\n              OR (runs.wake_step IS NULL AND (SELECT MIN(w.step_name)\n           FROM waits w\n           WHERE w.run_id = runs.run_id\n      AND w.queue = runs.queue\n      AND w.task_id = runs.task_id\n      AND w.event_name = runs.wake_event\n      AND w.status = 'waiting'\n      AND w.timeout_at_ms <=> runs.available_at_ms\n      AND (runs.available_at_ms IS NULL\n        OR ((runs.available_at_ms) IS NOT NULL AND runs.available_at_ms BETWEEN 0 AND 253402300799000))\n      AND (w.timeout_at_ms IS NULL\n        OR ((w.timeout_at_ms) IS NOT NULL AND w.timeout_at_ms BETWEEN 0 AND 253402300799000))\n           GROUP BY w.run_id\n            HAVING COUNT(*) = 1) IS NOT NULL)) and exists (select `f`.`queue` from `events` as `f` where `f`.`queue` = `runs`.`queue` and `f`.`event_name` = ? and `f`.`fence_stamp` = ?) and (EXISTS (SELECT 1 FROM tasks t\n                     WHERE t.task_id = runs.task_id AND t.queue = runs.queue AND t.state IN ('pending','running','sleeping')))",
        "bindArity": 15
      },
      {
        "sql": "update `tasks` set `state` = ('pending'), `fence_stamp` = ?, `fence_at_ms` = (select min(`f`.`fence_at_ms`) as `source_instant` from `runs` as `f` where (f.wake_event = ? AND f.state = 'pending') and `f`.`queue` = ? and `f`.`fence_stamp` = ?) where `task_id` in (select `f`.`task_id` from `runs` as `f` where (f.wake_event = ? AND f.state = 'pending') and `f`.`queue` = ? and `f`.`fence_stamp` = ?) and `tasks`.`queue` = ? and (state IN ('pending','running','sleeping'))",
        "bindArity": 8
      },
      {
        "sql": "delete from `waits` where `run_id` in (select `f`.`run_id` from `runs` as `f` where (f.queue = ? AND f.wake_event = ? AND f.state = 'pending') and `f`.`fence_stamp` = ?) and (event_name = ? AND status = 'waiting')",
        "bindArity": 4
      },
      {
        "sql": "update `runs` set `run_id` = `run_id`, `fence_stamp` = ?, `fence_at_ms` = (select min(`source_fence_at_ms`) as `source_instant` from (select distinct `f`.`fence_at_ms` as `source_fence_at_ms` from `runs` as `f` where (f.queue = ? AND f.wake_event = ? AND f.state = 'pending') and `f`.`fence_stamp` = ?) as `fenced_source_instant`) where `run_id` in (select `source_key` from (select distinct `f`.`run_id` as `source_key` from `runs` as `f` where (f.queue = ? AND f.wake_event = ? AND f.state = 'pending') and `f`.`fence_stamp` = ?) as `fenced_source`)",
        "bindArity": 7
      }
    ]
  },
  "sweep:cancel": {
    "cancelled": [
      {
        "sql": "update `tasks` set `state` = ?, `cancelled_at_ms` = COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n), `cancel_at_ms` = ?, `failure_reason` = ?, `fence_stamp` = ?, `fence_at_ms` = COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n) where `task_id` = ? and `queue` = ? and `state` in (?, ?, ?) and ((((tasks.cancel_at_ms) IS NOT NULL AND tasks.cancel_at_ms BETWEEN 0 AND 253402300799000)\n    AND tasks.cancel_at_ms <= COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n)) AND NOT EXISTS (\n    SELECT 1 FROM runs ownership_run\n    WHERE ownership_run.task_id = tasks.task_id\n      AND ownership_run.queue <> tasks.queue\n  ))",
        "bindArity": 9
      },
      {
        "sql": "update `runs` set `state` = ('cancelled'), `claimed_by` = (NULL), `claim_expires_at_ms` = (NULL), `fence_stamp` = ?, `fence_at_ms` = (select min(`f`.`fence_at_ms`) as `source_instant` from `tasks` as `f` where (f.task_id = ?) and `f`.`queue` = `runs`.`queue` and `f`.`fence_stamp` = ?) where `task_id` in (select `f`.`task_id` from `tasks` as `f` where (f.task_id = ?) and `f`.`queue` = `runs`.`queue` and `f`.`fence_stamp` = ?) and (state IN ('pending','running','sleeping'))",
        "bindArity": 5
      },
      {
        "sql": "delete from `waits` where `run_id` in (select `f`.`run_id` from `runs` as `f` where (f.task_id = ? AND f.state = 'cancelled') and `f`.`fence_stamp` = ?)",
        "bindArity": 2
      },
      {
        "sql": "insert into `events` (`queue`, `event_name`, `payload`, `emitted_at_ms`, `fence_stamp`, `fence_at_ms`) select `f`.`queue` as `queue`, ? as `event_name`, ? as `payload`, `f`.`fence_at_ms` as `emitted_at_ms`, ? as `fence_stamp`, `f`.`fence_at_ms` as `fence_at_ms` from `tasks` as `f` where `f`.`task_id` = ? and `f`.`queue` = ? and `f`.`fence_stamp` = ? and not exists (select `e`.`queue` from `events` as `e` where `e`.`queue` = `f`.`queue` and `e`.`event_name` = ?)",
        "bindArity": 7
      },
      {
        "sql": "update `runs` set `state` = ?, `wake_step` = coalesce(`wake_step`, ((SELECT MIN(w.step_name)\n           FROM waits w\n           WHERE w.run_id = runs.run_id\n      AND w.queue = runs.queue\n      AND w.task_id = runs.task_id\n      AND w.event_name = runs.wake_event\n      AND w.status = 'waiting'\n      AND w.timeout_at_ms <=> runs.available_at_ms\n      AND (runs.available_at_ms IS NULL\n        OR ((runs.available_at_ms) IS NOT NULL AND runs.available_at_ms BETWEEN 0 AND 253402300799000))\n      AND (w.timeout_at_ms IS NULL\n        OR ((w.timeout_at_ms) IS NOT NULL AND w.timeout_at_ms BETWEEN 0 AND 253402300799000))\n           GROUP BY w.run_id\n            HAVING COUNT(*) = 1))), `available_at_ms` = (select `f`.`fence_at_ms` from `events` as `f` where `f`.`queue` = `runs`.`queue` and `f`.`event_name` = ? and `f`.`fence_stamp` = ?), `wake_event` = ?, `event_payload` = (select `f`.`payload` from `events` as `f` where `f`.`queue` = `runs`.`queue` and `f`.`event_name` = ? and `f`.`fence_stamp` = ?), `fence_stamp` = ?, `fence_at_ms` = (select `f`.`fence_at_ms` from `events` as `f` where `f`.`queue` = `runs`.`queue` and `f`.`event_name` = ? and `f`.`fence_stamp` = ?) where `state` = ? and (wake_event = ?) and `run_id` in (SELECT w.run_id FROM waits w\n                        WHERE w.queue = ? AND w.event_name = ? AND w.status = 'waiting') and ((runs.wake_step IS NOT NULL AND EXISTS (SELECT 1 FROM waits w\n                      WHERE w.run_id = runs.run_id\n      AND w.queue = runs.queue\n      AND w.task_id = runs.task_id\n      AND w.event_name = runs.wake_event\n      AND w.status = 'waiting'\n      AND w.timeout_at_ms <=> runs.available_at_ms\n      AND (runs.available_at_ms IS NULL\n        OR ((runs.available_at_ms) IS NOT NULL AND runs.available_at_ms BETWEEN 0 AND 253402300799000))\n      AND (w.timeout_at_ms IS NULL\n        OR ((w.timeout_at_ms) IS NOT NULL AND w.timeout_at_ms BETWEEN 0 AND 253402300799000))\n                        AND w.step_name = runs.wake_step))\n              OR (runs.wake_step IS NULL AND (SELECT MIN(w.step_name)\n           FROM waits w\n           WHERE w.run_id = runs.run_id\n      AND w.queue = runs.queue\n      AND w.task_id = runs.task_id\n      AND w.event_name = runs.wake_event\n      AND w.status = 'waiting'\n      AND w.timeout_at_ms <=> runs.available_at_ms\n      AND (runs.available_at_ms IS NULL\n        OR ((runs.available_at_ms) IS NOT NULL AND runs.available_at_ms BETWEEN 0 AND 253402300799000))\n      AND (w.timeout_at_ms IS NULL\n        OR ((w.timeout_at_ms) IS NOT NULL AND w.timeout_at_ms BETWEEN 0 AND 253402300799000))\n           GROUP BY w.run_id\n            HAVING COUNT(*) = 1) IS NOT NULL)) and exists (select `f`.`queue` from `events` as `f` where `f`.`queue` = `runs`.`queue` and `f`.`event_name` = ? and `f`.`fence_stamp` = ?) and (EXISTS (SELECT 1 FROM tasks t\n                     WHERE t.task_id = runs.task_id AND t.queue = runs.queue AND t.state IN ('pending','running','sleeping')))",
        "bindArity": 15
      },
      {
        "sql": "update `tasks` set `state` = ('pending'), `fence_stamp` = ?, `fence_at_ms` = (select min(`f`.`fence_at_ms`) as `source_instant` from `runs` as `f` where (f.wake_event = ? AND f.state = 'pending') and `f`.`queue` = ? and `f`.`fence_stamp` = ?) where `task_id` in (select `f`.`task_id` from `runs` as `f` where (f.wake_event = ? AND f.state = 'pending') and `f`.`queue` = ? and `f`.`fence_stamp` = ?) and `tasks`.`queue` = ? and (state IN ('pending','running','sleeping'))",
        "bindArity": 8
      },
      {
        "sql": "delete from `waits` where `run_id` in (select `f`.`run_id` from `runs` as `f` where (f.queue = ? AND f.wake_event = ? AND f.state = 'pending') and `f`.`fence_stamp` = ?) and (event_name = ? AND status = 'waiting')",
        "bindArity": 4
      },
      {
        "sql": "update `runs` set `run_id` = `run_id`, `fence_stamp` = ?, `fence_at_ms` = (select min(`source_fence_at_ms`) as `source_instant` from (select distinct `f`.`fence_at_ms` as `source_fence_at_ms` from `runs` as `f` where (f.queue = ? AND f.wake_event = ? AND f.state = 'pending') and `f`.`fence_stamp` = ?) as `fenced_source_instant`) where `run_id` in (select `source_key` from (select distinct `f`.`run_id` as `source_key` from `runs` as `f` where (f.queue = ? AND f.wake_event = ? AND f.state = 'pending') and `f`.`fence_stamp` = ?) as `fenced_source`)",
        "bindArity": 7
      }
    ]
  },
  "sweep:lost-launch": {
    "swept": [
      {
        "sql": "update `runs` set `state` = ?, `claimed_by` = ?, `claim_expires_at_ms` = ?, `heartbeat_at_ms` = ?, `available_at_ms` = (COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n) + LEAST(\n      (relaunch_count + 1) * 5,\n      60\n    ) * 1000), `relaunch_count` = `relaunch_count` + ?, `fence_stamp` = ?, `fence_at_ms` = COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n) where `run_id` = ? and `queue` = ? and `state` = ? and `claim_gen` = ? and (activated_gen < claim_gen AND (((runs.claim_expires_at_ms) IS NOT NULL AND runs.claim_expires_at_ms BETWEEN 0 AND 253402300799000)\n    AND runs.claim_expires_at_ms <= COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n))) and `relaunch_count` < ? and (EXISTS (\n      SELECT 1 FROM tasks t\n      WHERE t.task_id = runs.task_id AND t.queue = runs.queue AND t.state IN ('pending','running','sleeping')\n        AND ((runs.claim_gen) IS NOT NULL AND runs.claim_gen BETWEEN 1 AND 1000000)\n   AND ((runs.activated_gen) IS NOT NULL AND runs.activated_gen BETWEEN 0 AND 1000000)\n   AND ((runs.relaunch_count) IS NOT NULL AND runs.relaunch_count BETWEEN 0 AND 5)\n   AND NOT EXISTS (SELECT 1 FROM (SELECT sibling_run.* FROM runs sibling_run\n    WHERE sibling_run.task_id = runs.task_id) sibling\n               WHERE sibling.state IN ('pending','running','sleeping')\n                 AND sibling.run_id <> runs.run_id)\n   AND (((runs.attempt) IS NOT NULL AND runs.attempt BETWEEN 1 AND 1000020)\n    AND ((t.attempts) IS NOT NULL AND t.attempts BETWEEN 0 AND 1000000)\n    AND ((t.max_attempts) IS NOT NULL AND t.max_attempts BETWEEN 1 AND 1000000)\n    AND ((t.infra_retries) IS NOT NULL AND t.infra_retries BETWEEN 0 AND 20)\n    AND t.attempts < t.max_attempts\n    AND runs.attempt = t.attempts + t.infra_retries + 1)\n   AND NOT EXISTS (\n    SELECT 1 FROM (SELECT higher_run.* FROM runs higher_run\n    WHERE higher_run.task_id = runs.task_id) higher\n    WHERE (NOT ((higher.attempt) IS NOT NULL AND higher.attempt BETWEEN 1 AND 1000020)\n        OR higher.attempt > runs.attempt)\n  )\n   AND (runs.activated_gen < runs.claim_gen\n     OR (runs.activated_gen = runs.claim_gen\n       AND (t.infra_retries = 20\n         OR (((runs.attempt) IS NOT NULL AND runs.attempt BETWEEN 1 AND 1000019)\n           AND runs.attempt = t.attempts + t.infra_retries + 1))))\n    )) and (((COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n)) IS NOT NULL\n    AND (COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n)) BETWEEN 0 AND 253402300799000\n    AND (COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n)) <= 253402300799000 - (CAST((LEAST(\n      (relaunch_count + 1) * 5,\n      60\n    ) * 1000) AS SIGNED))))",
        "bindArity": 11
      },
      {
        "sql": "update `runs` set `state` = ?, `failed_at_ms` = COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n), `failure_reason` = ?, `claimed_by` = ?, `claim_expires_at_ms` = ?, `fence_stamp` = ?, `fence_at_ms` = COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n) where `run_id` = ? and `queue` = ? and `state` = ? and `claim_gen` = ? and (activated_gen < claim_gen AND (((runs.claim_expires_at_ms) IS NOT NULL AND runs.claim_expires_at_ms BETWEEN 0 AND 253402300799000)\n    AND runs.claim_expires_at_ms <= COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n))) and `relaunch_count` = ? and (EXISTS (\n      SELECT 1 FROM tasks t\n      WHERE t.task_id = runs.task_id AND t.queue = runs.queue AND t.state IN ('pending','running','sleeping')\n        AND ((runs.claim_gen) IS NOT NULL AND runs.claim_gen BETWEEN 1 AND 1000000)\n   AND ((runs.activated_gen) IS NOT NULL AND runs.activated_gen BETWEEN 0 AND 1000000)\n   AND ((runs.relaunch_count) IS NOT NULL AND runs.relaunch_count BETWEEN 0 AND 5)\n   AND NOT EXISTS (SELECT 1 FROM (SELECT sibling_run.* FROM runs sibling_run\n    WHERE sibling_run.task_id = runs.task_id) sibling\n               WHERE sibling.state IN ('pending','running','sleeping')\n                 AND sibling.run_id <> runs.run_id)\n   AND (((runs.attempt) IS NOT NULL AND runs.attempt BETWEEN 1 AND 1000020)\n    AND ((t.attempts) IS NOT NULL AND t.attempts BETWEEN 0 AND 1000000)\n    AND ((t.max_attempts) IS NOT NULL AND t.max_attempts BETWEEN 1 AND 1000000)\n    AND ((t.infra_retries) IS NOT NULL AND t.infra_retries BETWEEN 0 AND 20)\n    AND t.attempts < t.max_attempts\n    AND runs.attempt = t.attempts + t.infra_retries + 1)\n   AND NOT EXISTS (\n    SELECT 1 FROM (SELECT higher_run.* FROM runs higher_run\n    WHERE higher_run.task_id = runs.task_id) higher\n    WHERE (NOT ((higher.attempt) IS NOT NULL AND higher.attempt BETWEEN 1 AND 1000020)\n        OR higher.attempt > runs.attempt)\n  )\n   AND (runs.activated_gen < runs.claim_gen\n     OR (runs.activated_gen = runs.claim_gen\n       AND (t.infra_retries = 20\n         OR (((runs.attempt) IS NOT NULL AND runs.attempt BETWEEN 1 AND 1000019)\n           AND runs.attempt = t.attempts + t.infra_retries + 1))))\n    ) OR EXISTS (\n      SELECT 1 FROM tasks t\n      WHERE t.task_id = runs.task_id AND t.queue = runs.queue AND t.state NOT IN ('pending','running','sleeping')\n        AND ((runs.claim_gen) IS NOT NULL AND runs.claim_gen BETWEEN 1 AND 1000000)\n   AND ((runs.activated_gen) IS NOT NULL AND runs.activated_gen BETWEEN 0 AND 1000000)\n   AND (runs.activated_gen = runs.claim_gen\n     OR (runs.activated_gen < runs.claim_gen\n       AND ((runs.relaunch_count) IS NOT NULL AND runs.relaunch_count BETWEEN 0 AND 5)\n       AND runs.relaunch_count = 5))\n    ))",
        "bindArity": 10
      },
      {
        "sql": "update `tasks` set `state` = ('pending'), `fence_stamp` = ?, `fence_at_ms` = (select min(`f`.`fence_at_ms`) as `source_instant` from `runs` as `f` where (f.run_id = ?) and `f`.`queue` = `tasks`.`queue` and `f`.`fence_stamp` = ?) where `task_id` in (select `f`.`task_id` from `runs` as `f` where (f.run_id = ?) and `f`.`queue` = `tasks`.`queue` and `f`.`fence_stamp` = ?) and (state IN ('pending','running','sleeping'))",
        "bindArity": 5
      },
      {
        "sql": "update `tasks` set `state` = ('failed'), `failure_reason` = (?), `fence_stamp` = ?, `fence_at_ms` = (select min(`f`.`fence_at_ms`) as `source_instant` from `runs` as `f` where (f.run_id = ?) and `f`.`queue` = `tasks`.`queue` and `f`.`fence_stamp` = ?) where `task_id` in (select `f`.`task_id` from `runs` as `f` where (f.run_id = ?) and `f`.`queue` = `tasks`.`queue` and `f`.`fence_stamp` = ?) and (state IN ('pending','running','sleeping'))",
        "bindArity": 6
      },
      {
        "sql": "delete from `waits` where `run_id` in (select `f`.`run_id` from `runs` as `f` where (f.run_id = ?) and `f`.`fence_stamp` = ?)",
        "bindArity": 2
      },
      {
        "sql": "insert into `events` (`queue`, `event_name`, `payload`, `emitted_at_ms`, `fence_stamp`, `fence_at_ms`) select `f`.`queue` as `queue`, ? as `event_name`, ? as `payload`, `f`.`fence_at_ms` as `emitted_at_ms`, ? as `fence_stamp`, `f`.`fence_at_ms` as `fence_at_ms` from `tasks` as `f` where `f`.`task_id` = ? and `f`.`queue` = ? and `f`.`fence_stamp` = ? and not exists (select `e`.`queue` from `events` as `e` where `e`.`queue` = `f`.`queue` and `e`.`event_name` = ?)",
        "bindArity": 7
      },
      {
        "sql": "update `runs` set `state` = ?, `wake_step` = coalesce(`wake_step`, ((SELECT MIN(w.step_name)\n           FROM waits w\n           WHERE w.run_id = runs.run_id\n      AND w.queue = runs.queue\n      AND w.task_id = runs.task_id\n      AND w.event_name = runs.wake_event\n      AND w.status = 'waiting'\n      AND w.timeout_at_ms <=> runs.available_at_ms\n      AND (runs.available_at_ms IS NULL\n        OR ((runs.available_at_ms) IS NOT NULL AND runs.available_at_ms BETWEEN 0 AND 253402300799000))\n      AND (w.timeout_at_ms IS NULL\n        OR ((w.timeout_at_ms) IS NOT NULL AND w.timeout_at_ms BETWEEN 0 AND 253402300799000))\n           GROUP BY w.run_id\n            HAVING COUNT(*) = 1))), `available_at_ms` = (select `f`.`fence_at_ms` from `events` as `f` where `f`.`queue` = `runs`.`queue` and `f`.`event_name` = ? and `f`.`fence_stamp` = ?), `wake_event` = ?, `event_payload` = (select `f`.`payload` from `events` as `f` where `f`.`queue` = `runs`.`queue` and `f`.`event_name` = ? and `f`.`fence_stamp` = ?), `fence_stamp` = ?, `fence_at_ms` = (select `f`.`fence_at_ms` from `events` as `f` where `f`.`queue` = `runs`.`queue` and `f`.`event_name` = ? and `f`.`fence_stamp` = ?) where `state` = ? and (wake_event = ?) and `run_id` in (SELECT w.run_id FROM waits w\n                        WHERE w.queue = ? AND w.event_name = ? AND w.status = 'waiting') and ((runs.wake_step IS NOT NULL AND EXISTS (SELECT 1 FROM waits w\n                      WHERE w.run_id = runs.run_id\n      AND w.queue = runs.queue\n      AND w.task_id = runs.task_id\n      AND w.event_name = runs.wake_event\n      AND w.status = 'waiting'\n      AND w.timeout_at_ms <=> runs.available_at_ms\n      AND (runs.available_at_ms IS NULL\n        OR ((runs.available_at_ms) IS NOT NULL AND runs.available_at_ms BETWEEN 0 AND 253402300799000))\n      AND (w.timeout_at_ms IS NULL\n        OR ((w.timeout_at_ms) IS NOT NULL AND w.timeout_at_ms BETWEEN 0 AND 253402300799000))\n                        AND w.step_name = runs.wake_step))\n              OR (runs.wake_step IS NULL AND (SELECT MIN(w.step_name)\n           FROM waits w\n           WHERE w.run_id = runs.run_id\n      AND w.queue = runs.queue\n      AND w.task_id = runs.task_id\n      AND w.event_name = runs.wake_event\n      AND w.status = 'waiting'\n      AND w.timeout_at_ms <=> runs.available_at_ms\n      AND (runs.available_at_ms IS NULL\n        OR ((runs.available_at_ms) IS NOT NULL AND runs.available_at_ms BETWEEN 0 AND 253402300799000))\n      AND (w.timeout_at_ms IS NULL\n        OR ((w.timeout_at_ms) IS NOT NULL AND w.timeout_at_ms BETWEEN 0 AND 253402300799000))\n           GROUP BY w.run_id\n            HAVING COUNT(*) = 1) IS NOT NULL)) and exists (select `f`.`queue` from `events` as `f` where `f`.`queue` = `runs`.`queue` and `f`.`event_name` = ? and `f`.`fence_stamp` = ?) and (EXISTS (SELECT 1 FROM tasks t\n                     WHERE t.task_id = runs.task_id AND t.queue = runs.queue AND t.state IN ('pending','running','sleeping')))",
        "bindArity": 15
      },
      {
        "sql": "update `tasks` set `state` = ('pending'), `fence_stamp` = ?, `fence_at_ms` = (select min(`f`.`fence_at_ms`) as `source_instant` from `runs` as `f` where (f.wake_event = ? AND f.state = 'pending') and `f`.`queue` = ? and `f`.`fence_stamp` = ?) where `task_id` in (select `f`.`task_id` from `runs` as `f` where (f.wake_event = ? AND f.state = 'pending') and `f`.`queue` = ? and `f`.`fence_stamp` = ?) and `tasks`.`queue` = ? and (state IN ('pending','running','sleeping'))",
        "bindArity": 8
      },
      {
        "sql": "delete from `waits` where `run_id` in (select `f`.`run_id` from `runs` as `f` where (f.queue = ? AND f.wake_event = ? AND f.state = 'pending') and `f`.`fence_stamp` = ?) and (event_name = ? AND status = 'waiting')",
        "bindArity": 4
      },
      {
        "sql": "update `runs` set `run_id` = `run_id`, `fence_stamp` = ?, `fence_at_ms` = (select min(`source_fence_at_ms`) as `source_instant` from (select distinct `f`.`fence_at_ms` as `source_fence_at_ms` from `runs` as `f` where (f.queue = ? AND f.wake_event = ? AND f.state = 'pending') and `f`.`fence_stamp` = ?) as `fenced_source_instant`) where `run_id` in (select `source_key` from (select distinct `f`.`run_id` as `source_key` from `runs` as `f` where (f.queue = ? AND f.wake_event = ? AND f.state = 'pending') and `f`.`fence_stamp` = ?) as `fenced_source`)",
        "bindArity": 7
      }
    ]
  },
  "sweep:claim-timeout": {
    "swept": [
      {
        "sql": "update `runs` set `state` = ?, `failed_at_ms` = COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n), `failure_reason` = ?, `claimed_by` = ?, `fence_stamp` = ?, `fence_at_ms` = COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n) where `run_id` = ? and `queue` = ? and `state` = ? and `claim_gen` = ? and (activated_gen = claim_gen AND (((runs.claim_expires_at_ms) IS NOT NULL AND runs.claim_expires_at_ms BETWEEN 0 AND 253402300799000)\n    AND runs.claim_expires_at_ms <= COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n))) and (EXISTS (\n           SELECT 1 FROM tasks t\n           WHERE t.task_id = runs.task_id AND t.queue = runs.queue\n             AND ((t.state NOT IN ('pending','running','sleeping')\n                 AND ((runs.claim_gen) IS NOT NULL AND runs.claim_gen BETWEEN 1 AND 1000000)\n   AND ((runs.activated_gen) IS NOT NULL AND runs.activated_gen BETWEEN 0 AND 1000000)\n   AND (runs.activated_gen = runs.claim_gen\n     OR (runs.activated_gen < runs.claim_gen\n       AND ((runs.relaunch_count) IS NOT NULL AND runs.relaunch_count BETWEEN 0 AND 5)\n       AND runs.relaunch_count = 5)))\n               OR (t.state IN ('pending','running','sleeping')\n                 AND ((runs.claim_gen) IS NOT NULL AND runs.claim_gen BETWEEN 1 AND 1000000)\n   AND ((runs.activated_gen) IS NOT NULL AND runs.activated_gen BETWEEN 0 AND 1000000)\n   AND ((runs.relaunch_count) IS NOT NULL AND runs.relaunch_count BETWEEN 0 AND 5)\n   AND NOT EXISTS (SELECT 1 FROM (SELECT sibling_run.* FROM runs sibling_run\n    WHERE sibling_run.task_id = runs.task_id) sibling\n               WHERE sibling.state IN ('pending','running','sleeping')\n                 AND sibling.run_id <> runs.run_id)\n   AND (((runs.attempt) IS NOT NULL AND runs.attempt BETWEEN 1 AND 1000020)\n    AND ((t.attempts) IS NOT NULL AND t.attempts BETWEEN 0 AND 1000000)\n    AND ((t.max_attempts) IS NOT NULL AND t.max_attempts BETWEEN 1 AND 1000000)\n    AND ((t.infra_retries) IS NOT NULL AND t.infra_retries BETWEEN 0 AND 20)\n    AND t.attempts < t.max_attempts\n    AND runs.attempt = t.attempts + t.infra_retries + 1)\n   AND NOT EXISTS (\n    SELECT 1 FROM (SELECT higher_run.* FROM runs higher_run\n    WHERE higher_run.task_id = runs.task_id) higher\n    WHERE (NOT ((higher.attempt) IS NOT NULL AND higher.attempt BETWEEN 1 AND 1000020)\n        OR higher.attempt > runs.attempt)\n  )\n   AND (runs.activated_gen < runs.claim_gen\n     OR (runs.activated_gen = runs.claim_gen\n       AND (t.infra_retries = 20\n         OR (((runs.attempt) IS NOT NULL AND runs.attempt BETWEEN 1 AND 1000019)\n           AND runs.attempt = t.attempts + t.infra_retries + 1))))\n                 AND (t.infra_retries = 20\n                   OR ((COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n)) IS NOT NULL\n    AND (COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n)) BETWEEN 0 AND 253402300799000\n    AND (COALESCE(\n  (SELECT CAST(value AS SIGNED) FROM meta WHERE `key` = 'fake_now_ms'),\n  CAST(TIMESTAMPDIFF(MICROSECOND, '1970-01-01 00:00:00', UTC_TIMESTAMP(6)) DIV 1000 AS SIGNED)\n)) <= 253402300799000 - (CAST((5 * 1000) AS SIGNED))))))\n         ))",
        "bindArity": 8
      },
      {
        "sql": "insert into `runs` (`run_id`, `queue`, `task_id`, `attempt`, `state`, `available_at_ms`, `created_at_ms`, `wake_event`, `event_payload`, `wake_step`, `run_db`, `fence_stamp`, `fence_at_ms`) select ? as `run_id`, `f`.`queue` as `queue`, `f`.`task_id` as `task_id`, `f`.`attempt` + ? as `attempt`, ? as `state`, `f`.`fence_at_ms` + ? as `available_at_ms`, `f`.`fence_at_ms` as `created_at_ms`, `f`.`wake_event` as `wake_event`, `f`.`event_payload` as `event_payload`, `f`.`wake_step` as `wake_step`, `f`.`run_db` as `run_db`, ? as `fence_stamp`, `f`.`fence_at_ms` as `fence_at_ms` from `runs` as `f` inner join `tasks` as `t` on (t.task_id = f.task_id AND t.queue = f.queue) where `f`.`run_id` = ? and `f`.`fence_stamp` = ? and (t.state IN ('pending','running','sleeping')\n         AND ((t.infra_retries) IS NOT NULL AND t.infra_retries BETWEEN 0 AND 20)\n         AND t.infra_retries < 20\n         AND ((f.attempt) IS NOT NULL AND f.attempt BETWEEN 1 AND 1000019)) and (NOT EXISTS (SELECT 1 FROM runs s\n           WHERE s.run_id = ? AND s.task_id = f.task_id\n             AND s.attempt = f.attempt + 1))",
        "bindArity": 8
      },
      {
        "sql": "update `tasks` set `state` = ('failed'), `failure_reason` = (?), `fence_stamp` = ?, `fence_at_ms` = (select min(`f`.`fence_at_ms`) as `source_instant` from `runs` as `f` where (f.run_id = ?) and `f`.`queue` = `tasks`.`queue` and `f`.`fence_stamp` = ?) where `task_id` in (select `f`.`task_id` from `runs` as `f` where (f.run_id = ?) and `f`.`queue` = `tasks`.`queue` and `f`.`fence_stamp` = ?) and (state IN ('pending','running','sleeping')\n            AND ((infra_retries) IS NOT NULL AND infra_retries BETWEEN 0 AND 20)\n            AND infra_retries = 20\n            AND NOT EXISTS (SELECT 1 FROM runs s\n           WHERE s.run_id = ? AND s.task_id = tasks.task_id\n             AND s.attempt = (SELECT p.attempt + 1 FROM runs p\n                WHERE p.run_id = ? AND p.fence_stamp = ?)))",
        "bindArity": 9
      },
      {
        "sql": "update `tasks` set `infra_retries` = ((SELECT f.attempt - 1 - tasks.attempts FROM runs f\n    WHERE f.run_id = ? AND f.fence_stamp = ?)), `state` = ('pending'), `last_attempt_run` = (?), `fence_stamp` = ?, `fence_at_ms` = (select min(`f`.`fence_at_ms`) as `source_instant` from `runs` as `f` where (f.run_id = ?) and `f`.`queue` = `tasks`.`queue` and `f`.`fence_stamp` = ?) where `task_id` in (select `f`.`task_id` from `runs` as `f` where (f.run_id = ?) and `f`.`queue` = `tasks`.`queue` and `f`.`fence_stamp` = ?) and (state IN ('pending','running','sleeping'))",
        "bindArity": 8
      },
      {
        "sql": "delete from `waits` where `run_id` in (select `f`.`run_id` from `runs` as `f` where (f.run_id = ?) and `f`.`fence_stamp` = ?)",
        "bindArity": 2
      },
      {
        "sql": "insert into `events` (`queue`, `event_name`, `payload`, `emitted_at_ms`, `fence_stamp`, `fence_at_ms`) select `f`.`queue` as `queue`, ? as `event_name`, ? as `payload`, `f`.`fence_at_ms` as `emitted_at_ms`, ? as `fence_stamp`, `f`.`fence_at_ms` as `fence_at_ms` from `tasks` as `f` where `f`.`task_id` = ? and `f`.`queue` = ? and `f`.`fence_stamp` = ? and not exists (select `e`.`queue` from `events` as `e` where `e`.`queue` = `f`.`queue` and `e`.`event_name` = ?)",
        "bindArity": 7
      },
      {
        "sql": "update `runs` set `state` = ?, `wake_step` = coalesce(`wake_step`, ((SELECT MIN(w.step_name)\n           FROM waits w\n           WHERE w.run_id = runs.run_id\n      AND w.queue = runs.queue\n      AND w.task_id = runs.task_id\n      AND w.event_name = runs.wake_event\n      AND w.status = 'waiting'\n      AND w.timeout_at_ms <=> runs.available_at_ms\n      AND (runs.available_at_ms IS NULL\n        OR ((runs.available_at_ms) IS NOT NULL AND runs.available_at_ms BETWEEN 0 AND 253402300799000))\n      AND (w.timeout_at_ms IS NULL\n        OR ((w.timeout_at_ms) IS NOT NULL AND w.timeout_at_ms BETWEEN 0 AND 253402300799000))\n           GROUP BY w.run_id\n            HAVING COUNT(*) = 1))), `available_at_ms` = (select `f`.`fence_at_ms` from `events` as `f` where `f`.`queue` = `runs`.`queue` and `f`.`event_name` = ? and `f`.`fence_stamp` = ?), `wake_event` = ?, `event_payload` = (select `f`.`payload` from `events` as `f` where `f`.`queue` = `runs`.`queue` and `f`.`event_name` = ? and `f`.`fence_stamp` = ?), `fence_stamp` = ?, `fence_at_ms` = (select `f`.`fence_at_ms` from `events` as `f` where `f`.`queue` = `runs`.`queue` and `f`.`event_name` = ? and `f`.`fence_stamp` = ?) where `state` = ? and (wake_event = ?) and `run_id` in (SELECT w.run_id FROM waits w\n                        WHERE w.queue = ? AND w.event_name = ? AND w.status = 'waiting') and ((runs.wake_step IS NOT NULL AND EXISTS (SELECT 1 FROM waits w\n                      WHERE w.run_id = runs.run_id\n      AND w.queue = runs.queue\n      AND w.task_id = runs.task_id\n      AND w.event_name = runs.wake_event\n      AND w.status = 'waiting'\n      AND w.timeout_at_ms <=> runs.available_at_ms\n      AND (runs.available_at_ms IS NULL\n        OR ((runs.available_at_ms) IS NOT NULL AND runs.available_at_ms BETWEEN 0 AND 253402300799000))\n      AND (w.timeout_at_ms IS NULL\n        OR ((w.timeout_at_ms) IS NOT NULL AND w.timeout_at_ms BETWEEN 0 AND 253402300799000))\n                        AND w.step_name = runs.wake_step))\n              OR (runs.wake_step IS NULL AND (SELECT MIN(w.step_name)\n           FROM waits w\n           WHERE w.run_id = runs.run_id\n      AND w.queue = runs.queue\n      AND w.task_id = runs.task_id\n      AND w.event_name = runs.wake_event\n      AND w.status = 'waiting'\n      AND w.timeout_at_ms <=> runs.available_at_ms\n      AND (runs.available_at_ms IS NULL\n        OR ((runs.available_at_ms) IS NOT NULL AND runs.available_at_ms BETWEEN 0 AND 253402300799000))\n      AND (w.timeout_at_ms IS NULL\n        OR ((w.timeout_at_ms) IS NOT NULL AND w.timeout_at_ms BETWEEN 0 AND 253402300799000))\n           GROUP BY w.run_id\n            HAVING COUNT(*) = 1) IS NOT NULL)) and exists (select `f`.`queue` from `events` as `f` where `f`.`queue` = `runs`.`queue` and `f`.`event_name` = ? and `f`.`fence_stamp` = ?) and (EXISTS (SELECT 1 FROM tasks t\n                     WHERE t.task_id = runs.task_id AND t.queue = runs.queue AND t.state IN ('pending','running','sleeping')))",
        "bindArity": 15
      },
      {
        "sql": "update `tasks` set `state` = ('pending'), `fence_stamp` = ?, `fence_at_ms` = (select min(`f`.`fence_at_ms`) as `source_instant` from `runs` as `f` where (f.wake_event = ? AND f.state = 'pending') and `f`.`queue` = ? and `f`.`fence_stamp` = ?) where `task_id` in (select `f`.`task_id` from `runs` as `f` where (f.wake_event = ? AND f.state = 'pending') and `f`.`queue` = ? and `f`.`fence_stamp` = ?) and `tasks`.`queue` = ? and (state IN ('pending','running','sleeping'))",
        "bindArity": 8
      },
      {
        "sql": "delete from `waits` where `run_id` in (select `f`.`run_id` from `runs` as `f` where (f.queue = ? AND f.wake_event = ? AND f.state = 'pending') and `f`.`fence_stamp` = ?) and (event_name = ? AND status = 'waiting')",
        "bindArity": 4
      },
      {
        "sql": "update `runs` set `run_id` = `run_id`, `fence_stamp` = ?, `fence_at_ms` = (select min(`source_fence_at_ms`) as `source_instant` from (select distinct `f`.`fence_at_ms` as `source_fence_at_ms` from `runs` as `f` where (f.queue = ? AND f.wake_event = ? AND f.state = 'pending') and `f`.`fence_stamp` = ?) as `fenced_source_instant`) where `run_id` in (select `source_key` from (select distinct `f`.`run_id` as `source_key` from `runs` as `f` where (f.queue = ? AND f.wake_event = ? AND f.state = 'pending') and `f`.`fence_stamp` = ?) as `fenced_source`)",
        "bindArity": 7
      }
    ]
  }
}
